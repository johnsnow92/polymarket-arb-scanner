# Calibration Report 2026-04-09 16:29 UTC

## Health
- Endpoint: UNREACHABLE (proxy 403 `host_not_allowed`)
- Last scan: N/A — status endpoint not reachable
- Scan count: N/A

### Connection Details
Both `/healthz` and `/status` returned HTTP 000 (exit code 56 — network error).
Verbose inspection shows the sandbox proxy returned `403 Forbidden` with
`x-deny-reason: host_not_allowed` when attempting to CONNECT to
`arb-scanner-production.up.railway.app:443`. This is an **execution-environment
network restriction**, not necessarily a bot outage — outbound connections to
Railway are blocked from within this sandbox.

## Opportunities
- Total found: N/A (status unreachable)
- With _layer tag: N/A
- Layer distribution: N/A
- Best ROI seen: N/A

## Trading
- Open positions: N/A
- Daily P&L: N/A
- DRY_RUN mode: N/A

## Status: UNREACHABLE

**Root cause**: The Claude Code execution sandbox blocks outbound HTTPS to
`*.railway.app` via proxy ACL (`host_not_allowed`). No status data could be
fetched. Manual verification required:

```bash
# Run from a machine with unrestricted network access:
curl -s -o /dev/null -w '%{http_code}' https://arb-scanner-production.up.railway.app/healthz
curl -s -u 'jtamm:Oaklandu15' https://arb-scanner-production.up.railway.app/status
```

**Next step**: Check Railway dashboard directly at https://railway.app or run
the curl commands above from a local terminal.
