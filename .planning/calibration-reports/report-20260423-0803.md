# Calibration Report 2026-04-23 08:03

## Health
- Endpoint: 403 (Forbidden) on `/healthz`
- Status endpoint: "Host not in allowlist" on `/status`
- Last scan: Unknown (status endpoint unreachable)
- Scan count: Unknown

## Connectivity Analysis
- `/healthz` returned HTTP 403 — endpoint is reachable (Railway app is running) but
  this environment's egress IP is not permitted by the app's allowlist configuration.
- `/status` returned body `"Host not in allowlist"` — the dashboard auth middleware
  is rejecting requests before returning JSON; same allowlist restriction.
- Root cause: Railway app enforces an IP allowlist (likely set via `ALLOWED_HOSTS`
  or a middleware guard in `dashboard.py`). The Railway runner's outbound IP is not
  in that list, or the check is keyed on the `Host` header rather than credentials.

## Opportunities
- Total found: Unknown (status unreachable)
- With _layer tag: Unknown
- Layer distribution: Unknown
- Best ROI seen: Unknown

## Trading
- Open positions: Unknown
- Daily P&L: Unknown
- DRY_RUN mode: Unknown

## Status: DOWN

## Recommended Actions
1. **Check Railway allowlist config** — review `dashboard.py` or `config.py` for
   `ALLOWED_HOSTS` / IP allowlist logic and either add the monitoring IP or disable
   the restriction for authenticated requests.
2. **Verify Railway service is running** — log into the Railway dashboard and confirm
   the service is deployed and the latest container is healthy.
3. **Re-run calibration** once access is restored to capture scan metrics and verify
   Phase 5 `_layer` field deployment.
