# Calibration Report 2026-04-08T16:09Z

## Health
- Endpoint: UNREACHABLE (proxy 403 host_not_allowed)
- Last scan: N/A (cannot connect)
- Scan count: N/A

## Connectivity Issue
The Claude Code sandbox environment routes all outbound HTTPS through an egress proxy
that enforces an allowlist. `arb-scanner-production.up.railway.app` is not on that
allowlist, so both the `/healthz` and `/status` requests were blocked with:

```
HTTP/1.1 403 Forbidden
x-deny-reason: host_not_allowed
```

This is a sandbox networking constraint, not a Railway service failure. To run
calibration checks manually:

```bash
curl -s -o /dev/null -w '%{http_code}' https://arb-scanner-production.up.railway.app/healthz
curl -s -u 'jtamm:Oaklandu15' https://arb-scanner-production.up.railway.app/status | jq .
```

## Opportunities
- Total found: N/A (no data)
- With _layer tag: N/A
- Layer distribution: N/A
- Best ROI seen: N/A

## Trading
- Open positions: N/A
- Daily P&L: N/A
- DRY_RUN mode: N/A

## Status: UNKNOWN
Cannot determine bot health — egress proxy blocked all outbound connections to Railway.
Run checks from a network environment with unrestricted outbound HTTPS.
