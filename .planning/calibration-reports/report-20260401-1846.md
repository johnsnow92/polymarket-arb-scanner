# Calibration Report 2026-04-01 18:46

## Health
- Endpoint: UNREACHABLE (proxy blocked — `403 host_not_allowed` from environment proxy)
- Last scan: N/A — status endpoint not reachable
- Scan count: N/A

## Network Note
All outbound HTTP/HTTPS requests from this Claude Code sandbox are routed through
an environment proxy that returns `403 Forbidden / x-deny-reason: host_not_allowed`
for external Railway URLs. This is a **sandbox networking restriction**, not a bot
failure. The Railway deployment itself may be healthy.

Health check attempted:
```
curl -s -o /dev/null -w '%{http_code}' https://arb-scanner-production.up.railway.app/healthz
→ curl exit 56 / HTTP 000 (CONNECT tunnel failed, proxy 403)
```

Status check attempted:
```
curl -s -u 'jtamm:Oaklandu15' https://arb-scanner-production.up.railway.app/status
→ Same proxy block
```

## Opportunities
- Total found: N/A
- With _layer tag: N/A (Phase 5 deployment status unknown)
- Layer distribution: N/A
- Best ROI seen: N/A

## Trading
- Open positions: N/A
- Daily P&L: N/A
- DRY_RUN mode: N/A

## Recommended Manual Checks
1. From a machine with direct internet access, run:
   ```bash
   curl -s -o /dev/null -w '%{http_code}' https://arb-scanner-production.up.railway.app/healthz
   curl -s -u 'jtamm:Oaklandu15' https://arb-scanner-production.up.railway.app/status | jq .
   ```
2. Check Railway dashboard for deployment status and recent logs.
3. Verify `last_scan_time` is within 15 minutes.
4. Verify `last_opportunities` entries contain `_layer` field (Phase 5 confirmation).

## Status: UNKNOWN (sandbox proxy blocked all outbound connections)
