# Calibration Report 2026-04-29 12:34

## Health
- Endpoint: 403 (Railway IP allowlist blocking this environment)
- Last scan: UNKNOWN — status endpoint unreachable
- Scan count: UNKNOWN

## Connectivity
Both `/healthz` and `/status` endpoints returned:
```
HTTP/2 403
Body: Host not in allowlist
```
TLS handshake with `arb-scanner-production.up.railway.app` completed successfully
(cert valid 2026-04-29 → 2026-05-29, issued by Railway Egress Gateway CA), so the
service is running and the domain resolves correctly. The 403 is from Railway's
edge-network IP allowlist — the Claude Code sandbox IP is not in the allowlist.

This is not a bot outage. The Railway project has IP-based ingress protection enabled
that prevents external IPs (including automated monitoring agents) from hitting the
dashboard. To fix this for future monitoring runs, either:
1. Add the Claude Code egress IP to Railway's ingress allowlist, or
2. Disable Railway's IP allowlist for the dashboard service, or
3. Use Railway's internal network / private networking for monitoring.

## Opportunities
- Total found: UNKNOWN
- With _layer tag: UNKNOWN (Phase 5 deployment unverifiable from this environment)
- Layer distribution: UNKNOWN
- Best ROI seen: UNKNOWN

## Trading
- Open positions: UNKNOWN
- Daily P&L: UNKNOWN
- DRY_RUN mode: UNKNOWN

## Status: DOWN (RAILWAY_IP_ALLOWLIST_BLOCKING)

**Root cause**: Railway edge-network IP allowlist returns 403 to all requests from
outside the allowlist. Service appears to be running (TLS/DNS healthy) but cannot
be inspected remotely from this monitoring environment.

**Action required**: Add monitoring IP to Railway allowlist or adjust ingress policy.
