# Calibration Report 2026-04-14 08:04 UTC

## Health
- Endpoint: UNREACHABLE (proxy 403 `host_not_allowed` — outbound HTTP blocked in this sandbox)
- Last scan: N/A — status endpoint could not be reached
- Scan count: N/A

## Opportunities
- Total found: N/A
- With _layer tag: N/A (cannot confirm Phase 5 deployment)
- Layer distribution: N/A
- Best ROI seen: N/A

## Trading
- Open positions: N/A
- Daily P&L: N/A
- DRY_RUN mode: N/A

## Network Diagnostics
- Health check command: `curl -s -o /dev/null -w '%{http_code}' https://arb-scanner-production.up.railway.app/healthz`
- Result: exit code 56, HTTP 000 (no connection)
- Proxy inspection: sandbox egress proxy returned `HTTP/1.1 403 Forbidden` with header `x-deny-reason: host_not_allowed`
- Root cause: Claude Code sandbox restricts outbound connections to non-whitelisted external hosts. The Railway deployment URL is blocked at the network layer.
- Status endpoint: `curl -s -u 'jtamm:Oaklandu15' https://arb-scanner-production.up.railway.app/status` — same 403 block.

## Remediation
To run this calibration check successfully:
1. Execute from a machine with unrestricted internet access (local terminal, Railway CLI shell, or a VPS).
2. Or add the Railway domain to the sandbox egress allowlist if operating in a controlled environment.
3. Alternatively, expose `/status` via Railway's built-in logging and query it through the Railway API/CLI (`railway logs`).

## Status: DOWN (UNREACHABLE FROM SANDBOX)

> Note: The Railway service itself may be healthy — this report only confirms that it cannot be
> reached from the current Claude Code sandbox environment. Manual verification is required.
