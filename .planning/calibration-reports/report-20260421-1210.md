# Calibration Report 2026-04-21 12:10

## Health
- Endpoint: 403 (health check /healthz returned Forbidden)
- Status endpoint: "Host not in allowlist" (Railway network restriction blocked /status fetch)
- Last scan: UNKNOWN (status endpoint unreachable)
- Scan count: UNKNOWN

## Opportunities
- Total found: UNKNOWN (status endpoint unreachable)
- With _layer tag: UNKNOWN/UNKNOWN
- Layer distribution: UNKNOWN
- Best ROI seen: UNKNOWN

## Trading
- Open positions: UNKNOWN
- Daily P&L: UNKNOWN
- DRY_RUN mode: UNKNOWN

## Errors
- `/healthz` returned HTTP 403 (Forbidden) — bot may be running but health endpoint is rejecting the request, or service is misconfigured
- `/status` returned "Host not in allowlist" — Railway is blocking outbound requests from this environment to the Railway app URL; this is a network/allowlist restriction, not necessarily a bot failure

## Status: DOWN

### Notes
Both endpoints are inaccessible from this environment. The 403 on `/healthz` is notable — a healthy bot should return 200. The "Host not in allowlist" message on `/status` suggests Railway's security policy is blocking access from this runner's IP. Recommend:
1. Check Railway dashboard logs directly to confirm bot process is running
2. Verify `/healthz` route in `dashboard.py` does not require auth (should be unauthenticated)
3. Check Railway service status page for outages
4. If bot is running, consider adding this runner's IP to the Railway allowlist, or use a Railway-internal URL for monitoring
