# Calibration Report 2026-04-30 16:28

## Health
- Endpoint: 403 (Forbidden) on `/healthz`
- Status endpoint: "Host not in allowlist" on `/status`
- Last scan: Unknown (status endpoint unreachable)
- Scan count: Unknown

## Connectivity Analysis
- TLS certificate issuer confirmed as `O=Anthropic; CN=sandbox-egress-production TLS Inspection CA` —
  outbound connections to `arb-scanner-production.up.railway.app` are intercepted and blocked by the
  Anthropic sandbox egress proxy before reaching Railway.
- `/healthz` returns HTTP 403 and `/status` returns `"Host not in allowlist"` — both are proxy-layer
  rejections, not responses from the Railway app itself.
- This is a persistent environment limitation: every calibration run since 2026-04-01 has hit the same
  block. The Railway app may be healthy; the sandbox simply cannot reach it.

## Opportunities
- Total found: Unknown (status unreachable)
- With _layer tag: Unknown
- Layer distribution: Unknown
- Best ROI seen: Unknown

## Trading
- Open positions: Unknown
- Daily P&L: Unknown
- DRY_RUN mode: Unknown

## Status: DOWN

## Recommended Actions
1. **Run calibration outside sandbox** — execute the two curl commands from a machine not behind the
   Anthropic egress proxy (e.g. local terminal, Railway shell, or a VPS):
   ```bash
   curl -s -o /dev/null -w '%{http_code}' https://arb-scanner-production.up.railway.app/healthz
   curl -s -u 'jtamm:Oaklandu15' https://arb-scanner-production.up.railway.app/status
   ```
2. **Verify Railway service** — log into Railway dashboard and confirm the service is deployed and
   the latest container is healthy.
3. **Phase 5 verification** — once `/status` is reachable, check `last_opportunities` for the
   `_layer` field on each entry to confirm Phase 5 code is live.
4. **Recurring block** — this is the 13th consecutive report with the same sandbox egress restriction.
   Consider automating calibration via a Railway cron job or GitHub Actions runner that has direct
   network access to the Railway service.
