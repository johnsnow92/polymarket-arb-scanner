# Calibration Report 2026-04-26 00:16 UTC

## Health
- Endpoint `/healthz`: **403 Forbidden** (server is responding, but access denied from this host)
- Endpoint `/status`: **"Host not in allowlist"** (IP-based allowlist is blocking this machine)
- Last scan: unknown (status blocked)
- Scan count: unknown (status blocked)

## Opportunities
- Total found: unknown (status blocked)
- With _layer tag: unknown (cannot confirm Phase 5 deployment)
- Layer distribution: unknown
- Best ROI seen: unknown

## Trading
- Open positions: unknown (status blocked)
- Daily P&L: unknown (status blocked)
- DRY_RUN mode: unknown

## Access Diagnosis
The Railway deployment is **online and responding** (HTTP responses received), but the
dashboard/status endpoints have an IP-based allowlist that blocks requests originating
from this environment. Specifically:
- `GET /healthz` → 403 (endpoint exists, IP not permitted)
- `GET /status` → plain-text "Host not in allowlist" (application-level guard, not HTTP auth)

The Basic Auth credentials (`jtamm:Oaklandu15`) are not the issue — the request is
rejected before auth is evaluated based on source IP.

## Recommended Actions
1. **Add this machine's egress IP to the Railway allowlist** (Railway Dashboard →
   Service → Networking, or update the `ALLOWED_HOSTS` / `DASHBOARD_ALLOWED_IPS` env var
   in `config.py` / `dashboard.py`).
2. Alternatively, run calibration checks from a host already on the allowlist (e.g.,
   the Railway shell, a VPN, or a whitelisted CI runner).
3. If the allowlist is managed in code, check `dashboard.py` for the host-check logic
   and verify the env var that controls it.

## Status: NEEDS_ATTENTION
> Bot appears running (HTTP responses received), but calibration cannot complete due to
> IP allowlist blocking all status/health data from this environment.
