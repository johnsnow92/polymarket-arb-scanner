# Calibration Report 2026-04-25 12:20

## Health
- Endpoint: 403 (Forbidden) on `/healthz`
- Status endpoint: "Host not in allowlist" on `/status`
- Last scan: Unknown (status endpoint unreachable)
- Scan count: Unknown

## Connectivity Analysis
- `/healthz` returned HTTP 403 — Railway service is reachable (TLS handshake succeeds
  to 66.33.22.21, cert valid until 2026-05-25) but this environment's egress IP is
  rejected before serving the health response.
- `/status` returned body `"Host not in allowlist"` — dashboard auth middleware rejects
  the request before returning JSON.
- TLS certificate signed by "Anthropic sandbox-egress-production TLS Inspection CA",
  confirming outbound traffic is intercepted by the Claude Code sandbox proxy.
- Root cause (persistent since 2026-04-01): The Railway app enforces an IP allowlist
  (via `dashboard.py` or `config.py`) that does not include the Anthropic sandbox
  egress IPs. This is the 8th consecutive report with the same block.

## Opportunities
- Total found: Unknown (status unreachable)
- With _layer tag: Unknown
- Layer distribution: Unknown
- Best ROI seen: Unknown

## Trading
- Open positions: Unknown
- Daily P&L: Unknown
- DRY_RUN mode: Unknown

## Status: DOWN

## Recommended Actions
1. **Fix allowlist** (blocking issue) — in `dashboard.py` or `config.py`, either:
   - Remove the IP allowlist check entirely for authenticated (`jtamm:Oaklandu15`) requests, OR
   - Add `0.0.0.0/0` (open) or the specific Anthropic sandbox CIDR to `ALLOWED_HOSTS`.
   The auth credentials already gate access — the IP check is redundant.
2. **Verify Railway service is alive** — log into Railway dashboard, confirm the
   container is running and most recent deploy succeeded.
3. **Re-run calibration** once access is restored to verify Phase 5 `_layer` field
   deployment and capture live scan metrics.
