---
phase: 05-deploy-execute
plan: 03
type: execute
wave: 3
depends_on: ["05-02"]
files_modified:
  - config.py
autonomous: false
requirements: [EXEC-01, EXEC-07]
user_setup:
  - service: railway
    why: "Production deployment platform"
    env_vars:
      - name: REVAL_FLOOR_L1
        source: "Set to 0.02 in Railway Dashboard -> Service -> Variables"
      - name: REVAL_FLOOR_L2
        source: "Set to 0.05"
      - name: REVAL_FLOOR_L3
        source: "Set to 0.03"
      - name: REVAL_FLOOR_L4
        source: "Set to 0.10"
      - name: MAX_TRADE_SIZE
        source: "Set to 5 for initial live trading (per D-08)"
      - name: DAILY_LOSS_LIMIT
        source: "Set to 25 for initial live trading (per D-09)"
must_haves:
  truths:
    - "Bot is deployed to Railway with all Phase 5 code changes"
    - "DRY_RUN=true during 72h calibration period"
    - "REVAL| structured logs appear in Railway logs showing layer-specific decisions"
    - "Revalidation pass rate is 5-30% during calibration (not 0% and not >50%)"
    - "At least one profitable round-trip trade is recorded in trades.db after going live"
  artifacts:
    - path: "config.py"
      provides: "Safety config defaults for initial live trading"
      contains: "MAX_TRADE_SIZE"
  key_links:
    - from: "Railway deployment"
      to: "GitHub master branch"
      via: "Push to master triggers auto-deploy"
      pattern: "git push origin master"
---

<objective>
Deploy to Railway, run 72h dry-run calibration, then enable live trading for the first profitable trade.

Purpose: All code changes (fee overhaul, layer tagging, revalidation floors, maker routing) are committed but not yet deployed. This plan pushes to master (triggering Railway auto-deploy), monitors the 72h dry-run calibration period to verify 5-30% revalidation pass rate, and then enables live trading with conservative safety limits ($5 max trade, $25/day loss limit per D-08/D-09).

Output: Production deployment, validated calibration, first profitable trade.
</objective>

<execution_context>
@C:\Users\jtamm\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\jtamm\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-deploy-execute/05-CONTEXT.md
@.planning/phases/05-deploy-execute/05-01-SUMMARY.md
@.planning/phases/05-deploy-execute/05-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update safety config defaults and push to master for Railway deployment</name>
  <files>config.py</files>
  <read_first>
    - config.py (verify MAX_TRADE_SIZE default, DAILY_LOSS_LIMIT default, DRY_RUN default)
    - .github/workflows/test.yml (verify CI runs tests before deploy)
  </read_first>
  <action>
1. Verify config.py defaults are safe for deployment:
   - `DRY_RUN` default must be `"true"` (it is: line ~119)
   - `MAX_TRADE_SIZE` default is currently `"3.0"` — update to `"5.0"` per D-08 (Railway env var can override)
   - `DAILY_LOSS_LIMIT` default is currently `"5.0"` — update to `"25.0"` per D-09
   - `EXECUTION_MODE` default must be `"semi-auto"` or `"full-auto"` — keep existing

2. Run the full test suite locally to confirm everything passes before push:
```bash
pytest tests/ -v --timeout=120
```

3. If tests pass, push to master:
```bash
git add -A
git commit -m "feat(phase5): fee overhaul, layer revalidation, maker routing

- Replace Polymarket 2% winner fee with dynamic taker fee P*(1-P)*rate
- Replace Gemini min(P,1-P)*0.05 with P*(1-P)*0.07 formula  
- Add kalshi_maker_fee() for maker routing cost calculations
- Add _layer tag to all scan modules (L1-L4)
- Implement layer-aware revalidation floors (2%/5%/3%/10%)
- Add structured REVAL| calibration logging
- Route Polymarket/Kalshi orders as maker (GTC) with timeout cancel
- Update safety defaults: MAX_TRADE_SIZE=5, DAILY_LOSS_LIMIT=25"
git push origin master
```

4. After push, verify Railway deployment triggers by checking GitHub Actions and Railway deploy status. The bot will start in DRY_RUN=true mode automatically.

5. Set the following env vars in Railway Dashboard (if not already set via defaults):
   - `REVAL_FLOOR_L1=0.02`
   - `REVAL_FLOOR_L2=0.05`
   - `REVAL_FLOOR_L3=0.03`
   - `REVAL_FLOOR_L4=0.10`
   - `DRY_RUN=true` (ensure this is explicitly set)
   - `LOG_LEVEL=INFO` (to capture REVAL| logs)
  </action>
  <verify>
    <automated>cd C:/Users/jtamm/Dev/polymarket-arb-scanner && git log --oneline -1 | grep -q "phase5" && echo "OK: commit exists" || echo "FAIL: commit missing"</automated>
  </verify>
  <acceptance_criteria>
    - config.py `MAX_TRADE_SIZE` default is `"5.0"`
    - config.py `DAILY_LOSS_LIMIT` default is `"25.0"`
    - config.py `DRY_RUN` default is `"true"`
    - `pytest tests/ -v` exits 0
    - `git log --oneline -1` shows commit with "phase5" or "fee" in message
    - `git push origin master` succeeds (triggers Railway deploy)
  </acceptance_criteria>
  <done>All Phase 5 code changes pushed to master. Railway auto-deploy triggered. Bot running in DRY_RUN=true mode with layer-aware revalidation and calibration logging active.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify 72h dry-run calibration results</name>
  <what-built>
    Bot deployed to Railway with:
    - Updated fee calculations (Polymarket dynamic taker, Gemini P*(1-P)*rate)
    - Layer-tagged opportunities (_layer 1-4 on every opp)
    - Layer-aware revalidation floors (2% L1, 5% L2, 3% L3, 10% L4)
    - Structured REVAL| calibration logging
    - Maker routing for Polymarket and Kalshi (GTC orders)
    - DRY_RUN=true (no actual trades)
  </what-built>
  <how-to-verify>
    After 72 hours of dry-run operation:

    1. Check Railway logs for REVAL| entries:
       - Search for "REVAL|layer=" in Railway Dashboard -> Service -> Logs
       - Verify logs contain all 4 layers (layer=1, layer=2, layer=3, layer=4)

    2. Calculate pass rate from logs:
       - Count lines with "passed=True" vs total "REVAL|" lines
       - Target: 5-30% overall pass rate (per D-04)
       - If 0%: floors too tight or fee calculation error
       - If >50%: floors too loose

    3. Check per-layer statistics:
       - Layer 1 should have the highest volume of opportunities
       - Layer 4 should have the fewest
       - Each layer's pass rate should be reasonable for its floor

    4. Verify no errors:
       - No Python tracebacks in Railway logs
       - No "unknown layer" warnings (means a scan module missed _layer tag)
       - Health check at /healthz returns 200

    5. Check fee calculations in logs:
       - Verify fee amounts in opportunity logs look reasonable
       - Polymarket fees should show P*(1-P) pattern (peak at P=0.50)
  </how-to-verify>
  <resume-signal>
    Type "approved" if pass rate is 5-30% and logs look correct.
    Type "floors need tuning" with specific layer adjustments if pass rate is off.
    Type "has errors" with details if there are tracebacks or issues.
  </resume-signal>
</task>

<task type="auto">
  <name>Task 3: Enable live trading with safety limits</name>
  <files>config.py</files>
  <read_first>
    - config.py (verify DRY_RUN, MAX_TRADE_SIZE, DAILY_LOSS_LIMIT, EXECUTION_MODE values)
    - Railway Dashboard env vars (current settings)
  </read_first>
  <action>
After the dry-run calibration is approved:

1. Set Railway env vars for live trading (per D-08, D-09):
```
DRY_RUN=false
MAX_TRADE_SIZE=5
DAILY_LOSS_LIMIT=25
EXECUTION_MODE=full-auto
```

These are set via Railway Dashboard, not code changes. The defaults in config.py already have MAX_TRADE_SIZE=5 and DAILY_LOSS_LIMIT=25.

2. Monitor for first trade:
   - Watch Railway logs for execution entries (not just REVAL| but actual trade placement)
   - Look for "Executed" or "Trade completed" log entries
   - Check trades.db via dashboard endpoint `/status` for recorded trades

3. Verify EXEC-07 success criteria:
   - At least one round-trip trade with net positive P&L in trades.db
   - Trade was autonomous (no human intervention)
   - P&L is recorded correctly (positive net after fees)

4. If the bot has been running for 24+ hours with no trades, check:
   - Maker order fill rate (are all GTC orders timing out?)
   - Opportunity detection rate (are opportunities being found?)
   - Revalidation pass rate (is it still in the target 5-30% range?)
   - Fee calculations (are fees making all opps unprofitable?)

Note: This task may need to wait hours or days for a profitable trade to appear. The bot must find a legitimate arbitrage opportunity, pass revalidation, successfully place and fill a maker order, and record the P&L. This is the final success gate for Phase 5.
  </action>
  <verify>
    <automated>cd C:/Users/jtamm/Dev/polymarket-arb-scanner && echo "Live trading enabled — verify via Railway Dashboard and /status endpoint. EXEC-07 requires at least one profitable round-trip trade in trades.db."</automated>
  </verify>
  <acceptance_criteria>
    - Railway env var DRY_RUN is set to "false"
    - Railway env var MAX_TRADE_SIZE is set to "5"
    - Railway env var DAILY_LOSS_LIMIT is set to "25"
    - Railway env var EXECUTION_MODE is set to "full-auto"
    - At least one trade record exists in trades.db with positive net P&L
  </acceptance_criteria>
  <done>Bot executing live trades autonomously with safety limits. At least one profitable round-trip trade recorded in trades.db (EXEC-07 satisfied).</done>
</task>

</tasks>

<verification>
1. Railway deployment is live and healthy (/healthz returns 200)
2. REVAL| structured logs flowing in Railway logs with all 4 layers
3. Revalidation pass rate 5-30% over 72h calibration period
4. DRY_RUN=false after calibration approval
5. At least one profitable autonomous trade in trades.db
</verification>

<success_criteria>
- Bot deployed to Railway with all Phase 5 changes (EXEC-01)
- 72h dry-run shows 5-30% revalidation pass rate (EXEC-01)
- Live trading enabled with $5 max trade / $25 daily loss limit (EXEC-07, D-08, D-09)
- At least one profitable round-trip trade recorded (EXEC-07, D-10)
</success_criteria>

<output>
After completion, create `.planning/phases/05-deploy-execute/05-03-SUMMARY.md`
</output>
