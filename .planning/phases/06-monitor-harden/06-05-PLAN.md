---
phase: 06-monitor-harden
plan: 05
type: execute
wave: 3
depends_on: []
files_modified:
  - tests/test_hedger.py
autonomous: true
requirements: [HARD-02]
must_haves:
  truths:
    - "Hedger executes partial fill scenarios on all 8 trading platforms without crashing"
    - "Partial fill detection works: leg 1 50% filled, leg 2 100% filled, hedges correctly"
    - "Hedger logs all hedge transactions with platform, leg, size, price"
    - "Hedger supports all platforms: Polymarket, Kalshi, Betfair, Smarkets, SX Bet, Matchbook, Gemini, IBKR"
  artifacts:
    - path: "tests/test_hedger.py"
      provides: "Hedger test suite with all 8 platform scenarios"
      pattern: "TestHedger.*Partial|all_platforms|polymarket.*kalshi.*betfair"
    - path: "hedger.py"
      provides: "Partial fill hedging for all 8 platforms (may already exist)"
      pattern: "def.*hedge|partial.*fill|execute_hedge"
    - path: "tests/test_hedger.py"
      provides: "Fixture mocking each platform client (8 fixtures)"
      min_lines: 80
  key_links:
    - from: "executor.py"
      to: "hedger.py"
      via: "Call hedger on partial fill detection"
      pattern: "hedger.*execute|hedge.*partial|fill.*<.*100"
    - from: "hedger.py"
      to: "all 8 platform clients"
      via: "Execute hedge order on opposite side"
      pattern: "polymarket_trader|kalshi_client|betfair_client|etc"
---

<objective>
Validate the hedger module on all 8 trading platforms with simulated partial fill scenarios. Create comprehensive tests that mock each platform's client, simulate a 50% fill on one leg of a 2-leg opportunity and a 100% fill on the other leg, and verify the hedger executes a counter-position on the 50% filled leg without crashing. Ensure hedger logs all actions and handles platform-specific execution paths correctly.

Purpose: Production reliability — ensure hedging logic works correctly when real orders partially fill
Output: Comprehensive hedger test suite covering all 8 platforms
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-monitor-harden/06-CONTEXT.md (HARD-02: Hedger validated on all 8 trading platforms)
@hedger.py
@executor.py (focus on partial fill detection and hedger call)
@tests/test_executor.py (for test pattern and platform mocking)
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create hedger test suite with fixtures for all 8 platforms</name>
  <files>tests/test_hedger.py</files>
  <read_first>
    @hedger.py (full file, understand Hedger class, execute_hedge method)
    @executor.py (lines ~300-400, focus on partial fill detection and hedger call)
    @tests/test_executor.py (for mock pattern and platform client fixtures)
  </read_first>
  <behavior>
    - Test 1: Polymarket partial fill (50% on YES, 100% on NO) → hedger sells YES on opposite market without error
    - Test 2: Kalshi partial fill → hedger executes short order without error
    - Test 3: Betfair partial fill → hedger lays opposite side without error
    - Test 4: Smarkets partial fill → hedger backs opposite side without error
    - Test 5: SX Bet partial fill → hedger executes hedge without error
    - Test 6: Matchbook partial fill → hedger executes without error
    - Test 7: Gemini partial fill → hedger executes without error
    - Test 8: IBKR partial fill (BUY-only platform) → hedger gracefully skips (no sell available)
    - Test 9: Hedger logs hedge transaction with: platform, leg, size, price, order_id
    - Test 10: Multiple partial fills (3+ legs) all hedged correctly (if applicable)
  </behavior>
  <action>
1. Create `tests/test_hedger.py`:
   - Class `TestHedgerPartialFills(unittest.TestCase)`
   - Autouse fixture `hedger()` — creates Hedger instance with mocked platform clients
   - Create 8 platform-specific fixtures (one per trading platform):
     - `mock_polymarket_trader()` — Mock PolymarketTrader with execute_order, get_order_status methods
     - `mock_kalshi_client()` — Mock KalshiClient with place_order, get_order methods
     - `mock_betfair_client()` — Mock BetfairClient with place_order, get_order_status methods
     - `mock_smarkets_client()` — Mock SmartetsClient with place_order, order_status methods
     - `mock_sxbet_client()` — Mock SXBetClient with place_order, order_status methods
     - `mock_matchbook_client()` — Mock MatchbookClient with place_order, order_status methods
     - `mock_gemini_client()` — Mock GeminiClient with place_order, get_order_status methods
     - `mock_ibkr_client()` — Mock IBKRClient with place_order method (BUY-only)

2. Test methods (one per platform):
   - `test_polymarket_partial_fill_hedge()`:
     - Create mock opportunity: 2 legs (YES, NO)
     - Simulate fills: leg 1 (YES) = 50% filled, leg 2 (NO) = 100% filled
     - Call hedger.execute_hedge(opportunity, fills={leg1: 0.5, leg2: 1.0})
     - Verify: polymarket_trader.execute_order called with SHORT order for YES
     - Verify: log contains "Polymarket hedge" + size + price
   - Same pattern for all 8 platforms
   - IBKR test: verify hedger gracefully skips (no sell order placed)

3. Each test:
   - Mock the platform client return value (order_id, status=FILLED, etc.)
   - Verify no exception raised
   - Verify correct order placed (correct platform, size, direction)
   - Verify logging includes hedge details

4. Use `unittest.mock.patch` to inject mocks into Hedger

5. All tests in class, no module-level functions

  </action>
  <verify>
    <automated>pytest tests/test_hedger.py::TestHedgerPartialFills -xvs</automated>
  </verify>
  <done>
    - Test file exists with TestHedgerPartialFills class
    - 8 test methods (one per platform) covering partial fill hedging
    - All mocks configured for respective platform clients
    - Tests verify no exceptions, correct order execution, logging
    - All tests pass
    - Code coverage for hedger execute_hedge method >90%
  </done>
</task>

<task type="auto">
  <name>Task 2: Verify hedger implementation supports all 8 platforms</name>
  <files>hedger.py</files>
  <read_first>
    @hedger.py (full file, check execute_hedge method for platform coverage)
  </read_first>
  <action>
1. In `hedger.py`, verify `execute_hedge()` method has branches for all 8 platforms:
   - Check for isinstance/if statements checking platform type: PolymarketTrader, KalshiClient, BetfairClient, SmartetsClient, SXBetClient, MatchbookClient, GeminiClient, IBKRClient
   - If any platform is missing: add the branch (follow existing pattern)

2. For each platform branch, verify:
   - Correct order direction (opposite of filled leg)
   - Size matches the partial fill amount
   - Price used is from price_cache or revalidation
   - Error handling present (try/except) — failed hedge doesn't crash bot
   - Logging includes platform, size, price, order result

3. Special handling for IBKR (BUY-only):
   - Verify that if a leg is filled via IBKR, and it was a BUY, the hedge (SELL) is gracefully skipped
   - Log: "IBKR hedge not available: BUY-only platform"

4. No changes needed if implementation already exists and covers all 8 platforms
   - If gaps found: add missing platform branches with same pattern

5. Verify hedger is called from executor.py after partial fill detection:
   - Executor detects fill_amount < order_amount
   - Calls: `self.hedger.execute_hedge(opp, fills={leg_id: fill_amount})`

  </action>
  <verify>
    <automated>grep -n "polymarket\|kalshi\|betfair\|smarkets\|sxbet\|matchbook\|gemini\|ibkr" hedger.py | grep -i "if\|isinstance\|elif" | wc -l</automated>
  </verify>
  <done>
    - hedger.py has execute_hedge() method with branches for all 8 platforms
    - Each branch handles partial fill and executes opposite order
    - IBKR branch gracefully skips (BUY-only)
    - Error handling present (try/except)
    - Logging includes platform, size, price
    - executor.py calls hedger after partial fill detection
  </done>
</task>

<task type="auto">
  <name>Task 3: Add test coverage for hedger error handling and edge cases</name>
  <files>tests/test_hedger.py</files>
  <read_first>
    @tests/test_hedger.py (from Task 1)
    @hedger.py (error handling patterns)
  </read_first>
  <action>
1. In `tests/test_hedger.py`, add additional test methods:
   - `test_hedger_handles_order_rejection()` — Mock platform client to return order_id=None (rejection), verify hedger logs warning and doesn't crash
   - `test_hedger_handles_network_timeout()` — Mock platform client to raise timeout exception, verify hedger catches it, logs error, returns False
   - `test_hedger_skips_if_no_partial_fill()` — All legs 100% filled, verify hedger is not called (or skipped gracefully)
   - `test_hedger_logs_all_hedges()` — Execute hedges on 2 platforms, verify log contains all hedge details (platform, size, price for each)

2. Use `unittest.mock.patch` with side_effect to simulate errors

3. Ensure all error cases log properly and don't crash bot

  </action>
  <verify>
    <automated>pytest tests/test_hedger.py -xvs | grep -i "error\|timeout\|rejection\|skip"</automated>
  </verify>
  <done>
    - Error handling tests added (network timeout, order rejection, no partial fill)
    - All error cases caught and logged without raising
    - Hedger gracefully handles edge cases
    - Logging comprehensive (all hedge details captured)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Hedger → Platform clients | Hedger calls platform-specific execute methods; trusts client returns correct order_id |
| Platform clients → Hedger | Returns order status; hedger trusts status is accurate |
| Hedger → Logging | Logs all hedges; no secrets in logs (no API keys, no balance details) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-16 | Denial of Service | Hedger loop executing repeatedly on same partial fill | mitigate | Executor marks hedge as completed after first execution; hedger not called again until new fill arrives |
| T-06-17 | Tampering | Hedge order size manually modified to exceed capital | mitigate | Hedger uses risk_manager to validate position size before execution (existing pattern) |
| T-06-18 | Information Disclosure | Hedger logs contain trade details (size, price, P&L) | mitigate | Trade details are acceptable for operator logging; no secrets revealed |

</threat_model>

<verification>
After all tasks complete:

1. Test all platforms: `pytest tests/test_hedger.py::TestHedgerPartialFills -xvs`
2. Test error handling: `pytest tests/test_hedger.py::TestHedgerErrorHandling -xvs` (if created)
3. Verify 8 platform branches: `grep -c "polymarket\|kalshi\|betfair\|smarkets\|sxbet\|matchbook\|gemini\|ibkr" hedger.py`
4. Verify logging: `grep -n "logger.*hedge\|logger.*info.*executed" hedger.py | head -5`
5. Verify executor calls hedger: `grep -n "hedger.*execute_hedge" executor.py`

Success: All 8 platform tests pass, hedger handles errors gracefully, logging comprehensive, executor integration verified
</verification>

<success_criteria>
- Test file `tests/test_hedger.py` exists with `TestHedgerPartialFills` class
- 8 test methods covering all platforms (Polymarket, Kalshi, Betfair, Smarkets, SX Bet, Matchbook, Gemini, IBKR)
- Each test simulates 50% fill on leg 1, 100% fill on leg 2; verifies hedger executes opposite order
- IBKR test verifies graceful skip (BUY-only platform)
- Error handling tests: order rejection, network timeout, no partial fill
- All tests pass: `pytest tests/test_hedger.py -xvs`
- Hedger implementation covers all 8 platforms in execute_hedge method
- Each platform branch handles partial fill, executes opposite order, logs result
- No exceptions raised on error (caught and logged)
- executor.py calls hedger after partial fill detection
- HARD-02 requirement satisfied: Hedger validated on all 8 trading platforms with simulated partial fill tests
</success_criteria>

<output>
After completion, create `.planning/phases/06-monitor-harden/06-05-SUMMARY.md` with:
- Objective summary (1-2 sentences)
- Key deliverables: Comprehensive hedger test suite covering all 8 platforms
- Files created/modified: tests/test_hedger.py
- Requirements addressed: HARD-02
- Test coverage: 8 platform-specific tests + 4 error handling tests = 12 tests, all passing
- Platforms validated: Polymarket, Kalshi, Betfair, Smarkets, SX Bet, Matchbook, Gemini, IBKR
</output>
