---
phase: 04-go-live
plan: 02
type: execute
wave: 2
depends_on:
  - "04-01"
files_modified: []
autonomous: false
requirements:
  - LIVE-03
  - LIVE-04
  - LIVE-05

must_haves:
  truths:
    - "Layer 2 strategies (resolution sniping, stale price, fee routing) execute alongside Layer 1"
    - "Layer 3 market making quotes are placed and filled with inventory within limits"
    - "Layer 4 informed trading strategies execute with signal quality validation"
    - "Net P&L remains positive after each layer addition"
  artifacts: []
  key_links:
    - from: "Railway env vars"
      to: "config.py"
      via: "MM_ENABLED=true, EVENT_MONITOR_ENABLED=true"
      pattern: "MM_ENABLED.*true"
---

<objective>
Progressively enable Layers 2, 3, and 4 on Railway with monitoring checkpoints between each.

Purpose: Incremental risk escalation — each layer adds riskier strategies only after confirming the previous layer is stable.
Output: All 20 strategies running in production.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-go-live/04-01-SUMMARY.md
@config.py
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Enable Layer 2 (near-arbitrage) on Day 2</name>
  <read_first>config.py (lines 230-310 for feature flags), scripts/pnl_report.py</read_first>
  <action>
Layer 2 strategies are already wired into continuous mode — they just need the bot to be running with `EXECUTION_MODE=full-auto` (already set in Plan 01). Layer 2 strategies (resolution sniping, stale price exploitation, fee promotional arbitrage) activate automatically when the continuous scan loop runs.

**Verify Layer 2 is active:**
1. Check Railway logs for resolution scan output: look for log lines containing "resolution" or "stale"
2. Check dashboard at GET `/status` — should show scan types including resolution and stale
3. Run P&L report to confirm Layer 1+2 combined:
   ```bash
   python scripts/pnl_report.py --db trades.db --hours 24
   ```

**Layer 2 requires no env var changes** — these strategies run automatically in continuous mode. The only confirmation needed is that they are actually executing and not erroring out.

**If resolution or stale strategies show errors:**
- Check that Kalshi and Polymarket credentials are valid
- Review `MIN_NET_ROI` threshold — resolution/stale opportunities may be below threshold
- Stale detection requires sufficient WebSocket history — may take 1-2 hours of uptime to start detecting

**Monitor for 24 hours after confirming Layer 2 is active.**
  </action>
  <acceptance_criteria>
- Railway logs show resolution scan and stale price scan executing without errors
- At least one resolution or stale opportunity detected (or confirmed zero exist in current market conditions)
- Combined Layer 1+2 P&L remains net positive
- No new error patterns introduced
  </acceptance_criteria>
  <resume-signal>Confirm Layer 2 strategies are active and P&L is stable, or describe issues</resume-signal>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Enable Layer 3 (market making) on Day 4</name>
  <read_first>config.py (lines 258-270 for MM config), market_maker.py (first 50 lines for MM params)</read_first>
  <action>
Set the following environment variables in Railway Dashboard:

```
MM_ENABLED=true
MM_MIN_SPREAD=0.02
MM_MAX_INVENTORY=500.0
```

These are the conservative defaults already in config.py. Setting them explicitly in Railway ensures they persist.

**After Railway redeploys, verify:**
1. Railway logs show MarketMaker initialization — look for "MarketMaker" or "market_maker" in logs
2. Dashboard at GET `/status` — should show MM-related activity
3. Check GET `/metrics` for market making counters (mm_quotes_placed, mm_fills, etc.)
4. Monitor inventory levels — should stay well within $500 limit per market

**Market making specific monitoring:**
- Watch for inventory buildup in one direction (bearish sign for MM strategy)
- Check that hedger is activating on partial fills
- Verify spread is being maintained at >= 2%

**Monitor for 24 hours. If inventory exceeds limits or P&L turns significantly negative:**
- Set `MM_ENABLED=false` to disable immediately
- Review which markets are causing losses
- Consider raising `MM_MIN_SPREAD` to 0.03 or 0.04 before re-enabling

Run P&L report:
```bash
python scripts/pnl_report.py --db trades.db --hours 24
```
  </action>
  <acceptance_criteria>
- `MM_ENABLED=true` is set in Railway
- Market making quotes are being placed (visible in logs or metrics)
- Inventory stays within $500/market limit
- Combined Layer 1+2+3 P&L remains net positive
- No crash loops or error patterns from MM engine
  </acceptance_criteria>
  <resume-signal>Confirm market making is active and stable, or describe issues</resume-signal>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3: Enable Layer 4 (informed trading) on Day 5</name>
  <read_first>config.py (lines 246-255 for event monitor config), event_monitor.py (first 50 lines), signal_aggregator.py (first 50 lines)</read_first>
  <action>
Set the following environment variable in Railway Dashboard:

```
EVENT_MONITOR_ENABLED=true
```

Optionally set `METACULUS_API_KEY` if not already present (improves rate limits but not required — public API works).

**After Railway redeploys, verify:**
1. Railway logs show EventMonitor initialization and Metaculus/Manifold API calls
2. Look for divergence signal detections in logs — "divergence" or "signal" keywords
3. Check convergence strategy is matching platform outliers

**Informed trading specific monitoring:**
- Layer 4 strategies (event divergence, cross-platform convergence, multi-source signal aggregation) are the riskiest
- These take directional bets, not hedged positions
- Watch P&L closely for the first 24 hours
- Signal quality: are divergence signals actually predictive? Check if trades based on signals are profitable

**If Layer 4 causes significant losses:**
- Set `EVENT_MONITOR_ENABLED=false` immediately
- Review `EVENT_DIVERGENCE_THRESHOLD` — may need to be raised (default 0.15 = 15% divergence required)
- Consider keeping Layer 4 off and running only Layers 1-3

Run P&L report for all layers:
```bash
python scripts/pnl_report.py --db trades.db --hours 24
```
  </action>
  <acceptance_criteria>
- `EVENT_MONITOR_ENABLED=true` is set in Railway
- Event monitor is fetching signals from Metaculus/Manifold without errors
- At least one divergence signal detected (or confirmed zero exist)
- All 4 layers running together without crashes
- P&L remains net positive across all layers combined
  </acceptance_criteria>
  <resume-signal>Confirm all 4 layers active and stable, or describe issues and which layers to keep/disable</resume-signal>
</task>

</tasks>

<verification>
- All feature flags enabled: MM_ENABLED=true, EVENT_MONITOR_ENABLED=true, SNAPSHOT_ENABLED=true, DYNAMIC_FEE_ENABLED=true
- All 20 strategies reachable in continuous mode
- No sustained error patterns in Railway logs
- Net positive P&L maintained through each layer addition
</verification>

<success_criteria>
All 4 strategy layers running in production on Railway. Each layer addition confirmed stable before proceeding to the next. All 20 strategies active.
</success_criteria>

<output>
After completion, create `.planning/phases/04-go-live/04-02-SUMMARY.md`
</output>
