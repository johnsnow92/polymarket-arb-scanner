# arbgrid Content Audit Register — 2026-05-31

> Evidence artifact for claudex plan loop `20260531-230458-iBXAqE`.
> Produced by a fresh-context `Explore` audit agent that cross-checked every `.md`/config claim
> against code on branch `feat/sprint-6-correlated-pairs` (head `7146988`, 2026-05-20).
> This is the canonical "what needs updating" register. Acceptance criterion #1 of PLAN.md
> is satisfied iff every row here is either fixed or explicitly deferred with a reason.

## Coverage / method
- **Inputs scanned:** `CLAUDE.md`, `README.md`, `docs/strategy-framework-v2.md`, `MEMORY.md`, `config.py`, all `*_api.py`, `cli.py` argparse choices, `tests/` inventory, working-tree git diff.
- **Inclusion rule:** a row is logged when a doc/config *claim* is contradicted by code, OR a conventional doc is absent, OR working-tree code is in a half-wired state.
- **Out of method:** runtime/behavioral correctness of strategies (not a code audit); only *claims vs. code* drift.

---

## A. Files needing updates (claim ≠ code)

| # | File / location | Stale claim | Code reality | Fix | Sev |
|---|---|---|---|---|---|
| A1 | `CLAUDE.md` "Current Status" (~L74–79) | "Last session 2026-04-14 … HubSpot: hubspot_accounts, hubspot_api"; "GSD Phase 9/9" | Head is 2026-05-20, 41 commits newer; active branch is Sprint 6 | Remove the rotting block; replace with a generated `STATE.md` pointer or a git-log pointer | **High** |
| A2 | `CLAUDE.md` Gemini fees (~L191) | "1% maker / 5% taker (`GEMINI_FEE_RATE`)" | `config.py` L156–157: `GEMINI_TAKER_RATE=0.07`, `GEMINI_MAKER_RATE=0.0175` (7% taker / 1.75% maker). `GEMINI_FEE_RATE=0.05` at L352 is **unused** | Doc: state "1.75% maker / 7% taker"; note old single-rate var superseded | **High** |
| A3 | `README.md` (~L35) | "22 BUILT, 6 PARTIAL, 1 STUB" | Per `docs/strategy-framework-v2.md` (2026-05-20): **26 BUILT, 3 PARTIAL, 0 STUB** | Update count; link to framework doc as source of truth | **Med** |
| A4 | `README.md` (~L59) | `--mode` list of 21 values | `cli.py` L1100–1105 wires ~20 MORE: `imbalance`, `news-snipe`, `correlated`, `time-decay`, `whale_copy`, `logical_arb`, `rewards`, `new_market`, `settlement_timing`, `api_outage`, `bracket`, `conditional`, `social_sentiment`, `expert_divergence`, `insider_pattern`, `cross_category`, `calibrated_signal`, `lead_lag_mm`, `toxic_flow_pause`, `volatility_adjusted_mm` | Regenerate the mode list from `cli.py` choices, grouped by layer | **Med** |
| A5 | **STRATEGY-COUNT INTEGRITY** (`CLAUDE.md`, `docs/strategy-framework-v2.md`) | Framework asserts "29 strategies / 5 layers" | `cli.py` exposes ~40 distinct modes (A4). The canonical "29" likely undercounts strategies that grew past the v2 framework | Reconcile the framework's strategy enumeration against the actual `cli.py` mode set; either fold extras into the taxonomy or document them as out-of-framework | **High** |
| A6 | `MEMORY.md` Metaculus note | "Metaculus API returns 403 to anonymous clients (breaks signal_aggregator/event_monitor)" | `metaculus_api.py` L65–89 handles missing API key gracefully; public API works (stricter rate limits); no 403 hardcoded or blocking | **Verify live**, then correct/remove the MEMORY claim. Do NOT propagate the 403 caveat into CLAUDE.md (would be a new error) | **Med** |
| A7 | `config.py` L352 | n/a | `GEMINI_FEE_RATE=0.05` is dead/unused | Remove or mark deprecated — **code change, separate PR** (not the doc PR) | **Low** |

## B. Missing documents to create

| Doc | Justification |
|---|---|
| `docs/ROADMAP.md` | No versioned milestone view; PLAN.md is session-scoped. Needed for Layer-5 completion, platform expansion, #6/#18/#20 remediation. |
| `docs/ARCHITECTURE.md` | CLAUDE.md is dev-guidance; a standalone design doc (data flow, per-layer responsibility, deploy topology, key decisions) serves newcomers + risk review. |
| `docs/PLATFORM-MATRIX.md` (a.k.a. INTEGRATION-STATUS) | Platform × capability matrix (auth, buy/sell, streaming, fees, EIP-712, flag, BUILT/PARTIAL/STUB). **Becomes the single source of truth** that CLAUDE.md + framework table reference. |
| `docs/RISK-POLICY.md` | One audit-ready doc for position limits, daily-loss breaker, reentry rules, partial-fill hedge, `validate_config()` gates. |
| `docs/RUNBOOK.md` (DEPLOYMENT) | Railway deploy, env-var setup (`DASHBOARD_HOST=0.0.0.0`, `DASHBOARD_PASS`), IB Gateway, credential rotation, smoke tests, safe flag-enable. |
| `docs/BACKTESTING.md` | Snapshot recording, replay/fill semantics, #20 tuning loop, how thresholds are tuned + validated. |
| `docs/SECURITY.md` | Secrets handling, rotation, redaction, dashboard auth, Polymarket-wallet-drain class of risk. |
| `CHANGELOG.md` | Versioned history; git log is current truth but lacks release notes. (Source-of-truth: GitHub PR metadata vs local log — TBD.) |
| `CONTRIBUTING.md` | Branch naming, PR checklist, test expectations, "how to add a strategy" recipe. |
| `TASK_CONTRACT.md` | Formalize opportunity-dict schema, execution-leg format, the 3 custom exceptions. Decision pending. |

## C. In-flight / half-done work

- **Strategy #20 backtest tuning loop — IMPLEMENTATION-COMPLETE, NOT PRODUCTION-WIRED.** Working tree: `backtest.py` (+`_suggest_strategy_thresholds`, expanded `build_recommendations`), `config.py` (+`BACKTEST_TUNING_ENABLED`, `load/apply_backtest_recommendations`, `RECOMMENDED_BY_STRATEGY`), `scripts/tune.py` (dual output modes). New tests `tests/test_backtest_tuning.py` + `tests/test_config_tuning_loader.py` (paired with real impl — **not orphaned**). Gaps: not wired into `continuous.py` auto-tuning; no alert on recommendation change. **Framework lists #20 as PARTIAL/pending — docs should move it toward BUILT once wired.**
- **Correlated pairs (#29) — COMPLETE.** `scans/correlated.py`, `correlation_tracker.py`, `cli.py` L687–732, `executor.py` L1844–1852, tests (`test_correlated*.py`, 58 passing). Despite the branch name, this is done; the branch's *active* delta is the #20 tuning work above.
- `data/snapshots.db` + `data/backtest_recommendations.json` present (untracked) — generated artifacts the tuning loop consumes.

## D. Verified-accurate (no action)
- "8 trading platforms + 2 read-only signal sources" — accurate (table below).
- SX Bet #6 — still READ-ONLY/quarantined; `place_order()` unsigned JSON; `validate_config()` blocks live when `DRY_RUN=false`. Accurate.
- Test suite: 96 test files; no `skip`/`xfail`/`flaky` decorators found.

### Platform client status
| Platform | Module | State |
|---|---|---|
| Polymarket | `polymarket_api.py` | Working (buy/sell) |
| Kalshi | `kalshi_api.py` | Working |
| Betfair | `betfair_api.py` | Working |
| Smarkets | `smarkets_api.py` | Working |
| SX Bet | `sxbet_api.py` | **Read-only / quarantined** (no EIP-712) |
| Matchbook | `matchbook_api.py` | Working (0% commission) |
| Gemini | `gemini_api.py` | Working (7% taker / 1.75% maker) |
| IBKR ForecastEx | `ibkr_api.py` | Working — BUY-only, LMT-only |
| Metaculus | `metaculus_api.py` | Read-only (anon OK; see A6) |
| Manifold | `manifold_api.py` | Read-only |
