# Codebase Audit Report — Arbgrid / polymarket-arb-scanner

**Generated:** 2026-06-27 16:17:41 UTC
**Scope:** Python, 242 files.
**Total confirmed findings:** 182 (0 unverified)

---

## Executive Summary

182 confirmed findings across all severity bands. No findings errored out.

| Severity | Count |
|---|---|
| High | 16 |
| Medium | 88 |
| Low | 78 |

**One-line verdict:** The bot has multiple production-blocking bugs — two non-reentrant lock deadlocks that fire immediately in live mode, five scan-layer refiners that never filter losing candidates (passing false arbs to the executor), a P&L settlement formula that returns negative numbers on multi-contract positions, and a CI configuration that silently passes on test errors — alongside 88 medium findings covering security controls, fee-calculation correctness, and test-validity gaps.

**Remediation priority (top 10):**
1. Fix two deadlocks (TaxOptimizer, RegionRouter) — process hangs in production
2. Fix _refine_cross_all_with_clob / _refine_triangular_with_clob filter holes — executor receives loss-making trades
3. Fix _calc_realized_pnl unit-scale formula — P&L in DB wrong for every multi-contract arb
4. Fix CI test-error detection — currently blind to erroring test suites
5. Fix idempotency double-spend in treasury — real funds sent twice under concurrency
6. Fix WhaleCopy position counter never decrementing — strategy locks out permanently after N fills
7. Fix FeedManager not sharing price_cache — stale-feed detection silently dead
8. Fix Sharpe ratio and drawdown formulas in analytics — metrics actively misleading
9. Fix proxy URL SSRF across six API clients
10. Fix tautological / vacuous test assertions (at least 12 confirmed)

---

## HIGH Findings (16)

### H-01 — Deadlock: TaxOptimizer.get_harvest_candidates calls is_long_term while holding non-reentrant Lock
**File:** `capital_optimizer.py:313`
**Evidence:**
```python
with self._lock:  # line 302 — threading.Lock (not RLock)
    rate = TAX_LONG_TERM_RATE if self.is_long_term(position_id) ...
# is_long_term (line 256):
def is_long_term(self, position_id):
    with self._lock:  # deadlocks — same non-reentrant lock
```
**Impact:** Any call to `get_harvest_candidates` when `TAX_AWARE_ENABLED=true` permanently hangs the calling thread. On Railway (24/7 process with ThreadPoolExecutor), that worker is lost forever.
**Fix:** Change `self._lock = threading.Lock()` to `self._lock = threading.RLock()`, or extract an internal `_is_long_term_unlocked` helper called from within the lock.

---

### H-02 — Deadlock: RegionRouter.should_route_externally calls get_best_region while holding non-reentrant Lock
**File:** `latency_monitor.py:366`
**Evidence:**
```python
with self._lock:
    region_data = self._region_latencies.get(platform, {})
    best_region = self.get_best_region(platform)  # also acquires self._lock
```
**Impact:** First call when `GEOGRAPHIC_LATENCY_ENABLED=True` deadlocks permanently.
**Fix:** Change `self._lock` to `threading.RLock()`, or inline the `min()` computation from `get_best_region` without the nested lock.

---

### H-03 — _refine_cross_all_with_clob never removes unprofitable candidates
**File:** `scans/cross.py:621`
**Evidence:**
```python
refined_out = []  # declared but never appended to and never returned
for o in pm_opps:
    if best_r["net_profit"] >= min_profit:
        o["net_profit"] = best_r["net_profit"]
    # else: o stays in opportunities with stale mid-price net_profit
```
**Impact:** Stage 2 CLOB refinement is cosmetic for cross-all opportunities. Loss-making candidates (real ask-price profit < min_profit) pass to the executor unchanged, causing real trades on what are actually losing positions.
**Fix:** Build `keep_ids = {id(o) for o in refined_out}`; at end: `opportunities[:] = [o for o in opportunities if id(o) not in {id(x) for x in pm_opps} or id(o) in keep_ids]`.

---

### H-04 — TriangularCross CLOB refinement uses 0 for non-PM leg price (missing _price_a/_price_b keys)
**File:** `scans/triangular.py:414`
**Evidence:**
```python
other_price = o.get("_price_b", 0)  # always 0 — key never set on TriangularCross opps
# TriangularCross opp dict (lines 311-326) never sets _price_a, _price_b, _side_a, _side_b
```
**Impact:** `net_profit_triangular` is called with `other_price=0`, producing inflated ROI. Every triangular arb passes CLOB refinement with a fictitious profit figure.
**Fix:** Add to the TriangularCross opp dict: `"_price_a": best_yes, "_price_b": best_no, "_side_a": "yes", "_side_b": "no"`.

---

### H-05 — TriangularCross CLOB refinement selects wrong CLOB side when PM is the NO leg
**File:** `scans/triangular.py:422`
**Evidence:**
```python
pm_side = o.get("_side_b", "yes") if "_side_b" in o else "yes"
# _side_b never set on TriangularCross → pm_side always "yes"
# but PM may be the NO leg → clob["yes_ask"] used instead of clob["no_ask"]
```
**Impact:** Compounds H-04. Wrong CLOB price used for the PM NO leg, further corrupting profit recalculation.
**Fix:** Set `_side_a='yes'` and `_side_b='no'` in the TriangularCross opp dict (same fix as H-04).

---

### H-06 — correlated: CLOB fetch_tasks indexes market dict by wrong key when long/short legs are swapped
**File:** `scans/correlated.py:367`
**Evidence:**
```python
for key, market_field in (
    (opp.get("_market_key_a"), opp.get("_long_market")),  # key_a may map to market_b dict
    (opp.get("_market_key_b"), opp.get("_short_market")),
):
    fetch_tasks[key] = market_field
```
**Impact:** When `price_a >= price_b` causes legs to swap, `clob_results[market_key_a]` holds market_b's CLOB. `long_ask` and `short_bid` are read from the opposite market — depth checks and live spread calculations use inverted data.
**Fix:** Build `fetch_tasks` using logical role keys: `fetch_tasks[opp.get("_long_leg")] = opp.get("_long_market")`.

---

### H-07 — analytics.py: annual_sharpe is stddev × √252 (annualised volatility), not Sharpe ratio
**File:** `scripts/analytics.py:106`
**Evidence:**
```sql
THEN stddev * SQRT(252)  -- missing division by mean
END as annual_sharpe
```
**Impact:** Every strategy's "Sharpe" metric is actually annualised volatility. Higher volatility incorrectly shows as higher Sharpe. All downstream threshold checks, strategy rankings, and alerts based on this field are wrong.
**Fix:**
```sql
CASE WHEN trade_count >= 20 AND stddev IS NOT NULL AND stddev > 0
    THEN (avg_pnl / stddev) * SQRT(252)
    ELSE NULL
END as annual_sharpe
```

---

### H-08 — CI: pytest "errors" plural never matched — test errors silently pass CI
**File:** `.github/workflows/test.yml:35`
**Evidence:**
```bash
SUMMARY=$(tail -5 test-output.txt | grep -oP '\d+ (failed|error)' || echo "")
# pytest prints "2 errors", not "2 error" — pattern never captures error count
ERRORS=$(... grep -oP '(\d+) error' ...)  # always "0"
```
**Impact:** A run with 5 test errors and 0 failures produces `TOTAL_BAD=0` and exits 0. CI passes silently despite broken tests — zero-tolerance policy violation per CLAUDE.md.
**Fix:** Change pattern to `'\d+ (failed|errors)'`; or rely on pytest's own exit code: `pytest ...; exit ${PIPESTATUS[0]}`.

---

### H-09 — Treasury idempotency: live withdrawal executes twice before DB UNIQUE constraint fires
**File:** `tests/test_treasury.py:186` (documents production behaviour)
**Evidence:**
```python
# Test comment: "the live withdraw is called twice (the gate is the DB UNIQUE constraint),
# but the audit table still has exactly one row."
```
**Impact:** Under concurrent execution (two simultaneous cron triggers or retries), two real Gemini `withdraw_usdc` calls complete before either commits to the DB. Only the second INSERT is rolled back — the funds are already sent twice.
**Fix:** Acquire a lock before the DB insert and hold it through the Gemini API call. Insert the pending row first; check for UNIQUE collision before calling the API.

---

### H-10 — Treasury idempotency test: withdraw_usdc never asserted to be called only once
**File:** `tests/test_treasury.py:176`
**Evidence:**
```python
# Test name: test_replay_returns_existing_id_without_double_execution
assert r1.transfer_id == r2.transfer_id  # only checks ID, not call count
# gemini.withdraw_usdc.assert_called_once() is never called
```
**Impact:** A double-spend regression passes CI silently.
**Fix:** Add `gemini.withdraw_usdc.assert_called_once_with(...)` after the second call.

---

### H-11 — FeedManager created without shared price_cache — stale-feed detection permanently broken
**File:** `continuous.py:1044`
**Evidence:**
```python
feed_manager = FeedManager(
    on_price_update=on_price_update,
    # price_cache=price_cache NOT passed
)
# FeedManager.__init__: self._price_cache = {}  (own empty dict)
# mark_stale_feeds iterates self._price_cache — always empty — never fires
```
**Impact:** The entire staleness monitoring subsystem is silently dead. Stale prices from disconnected feeds are never flagged.
**Fix:** Pass `price_cache=price_cache` to the `FeedManager` constructor.

---

### H-12 — WhaleCopy position counter never decremented — permanent strategy lockout
**File:** `executor.py:2652`
**Evidence:**
```python
self._whale_copy_position_count += 1  # incremented on fill, never decremented
if self._whale_copy_position_count >= WHALE_COPY_MAX_POSITIONS:
    return False  # permanent after N fills
```
**Impact:** On Railway (24/7 process), WhaleCopy permanently disables itself after `WHALE_COPY_MAX_POSITIONS` fills (default 5). Settled positions never release the slot.
**Fix:** Decrement in `check_settlements()` when a WhaleCopy position settles, or query `db.get_open_positions()` filtered by `type='WhaleCopy'` dynamically.

---

### H-13 — polymarket_api circuit breaker triggered by non-retriable 4xx/5xx HTTP errors
**File:** `polymarket_api.py:77`
**Evidence:**
```python
except Exception:           # catches HTTPError (404, 500, etc.)
    _circuit.record_failure()  # 3 such calls opens circuit for 30s
    raise
```
**Impact:** A legitimate 404 (inactive market) counted 3 times opens the circuit and blocks ALL Polymarket requests for 30s. The circuit breaker designed to handle real outages is triggered by normal operational responses.
**Fix:**
```python
except (_RateLimitError, requests.ConnectionError, requests.Timeout):
    _circuit.record_failure(); raise
except requests.HTTPError:
    raise  # do not penalise circuit for authoritative HTTP errors
```

---

### H-14 — _refine_cross_all_with_clob: losing candidates never filtered (second confirmation)
**File:** `scans/cross.py:621`

Same root cause as H-03, confirmed from a second evidence angle: `refined_out` is never returned and the function has no return statement. The `net_roi` string is also never updated for passing candidates. Fix: see H-03.

---

### H-15 — recovery.py passes order_id as token_id to log_partial_fill, breaking hedge lookup
**File:** `recovery.py:266`
**Evidence:**
```python
db.log_partial_fill(
    ...
    token_id=trade.get("order_id", ""),  # BUG: order_id ≠ token_id
)
```
**Impact:** The Polymarket CLOB is keyed by numeric token ID (e.g. `71321045...`), not order UUIDs. After a crash, `_hedge_polymarket(token_id, ...)` calls `fetch_order_book(order_uuid)` and receives nothing — no orphaned Polymarket position can be hedged.
**Fix:** `token_id=trade.get('token_id', trade.get('order_id', ''))`.

---

### H-16 — _calc_realized_pnl assumes $1 total payout regardless of contract count
**File:** `continuous.py:262`
**Evidence:**
```python
if winning_side is None:
    return 1.0 - total_fill_cost
# With size=5 contracts at prices 0.40/0.55:
# total_fill_cost = 4.75; returns 1.0 - 4.75 = -$3.75
# Correct answer: ~+$0.25
```
**Impact:** Every multi-contract arb has its settled P&L recorded as a large negative number. The daily-loss guard reads this and may block legitimate trading. The test `test_uses_fill_prices_when_available` validates the wrong formula (-3.35).
**Fix:** `winning_contracts = best_leg['size'] / (best_leg.get('fill_price') or best_leg['price']); return winning_contracts - total_fill_cost`.

---

## MEDIUM Findings (88)

### M-01 — _select_fees inverts EV probability weights for case_a and case_b
**File:** `fees.py:34`
```python
return prob_a * case_b_fees + (1.0 - prob_a) * case_a_fees  # swapped
```
**Fix:** `return prob_a * case_a_fees + (1.0 - prob_a) * case_b_fees`.

### M-02 — get_daily_pnl mixes realized P&L with unrealized expected_pnl from open positions
**File:** `db.py:252`
**Impact:** Daily-loss guard reads falsely positive P&L; allows more trades while the bot is losing.
**Fix:** Exclude open positions; return only settled `realized_pnl`.

### M-03 — ws_feeds.py: mark_stale_feeds treats never-messaged feeds as fresh
**File:** `ws_feeds.py:241`
```python
last_msg_time = self._last_message_time.get(platform, now)  # should be 0
```
**Fix:** `self._last_message_time.get(platform, 0)`.

### M-04 — alerting.py: check_strategy_loss_streak fires only on exactly 3 losses
**File:** `alerting.py:324`
```python
if losses == 3:  # should be >= 3
```
**Fix:** `if losses >= 3:`.

### M-05 — backtest.py: NegRisk fee recalculation passes only 2 prices for multi-outcome function
**File:** `backtest.py:322`
**Fix:** Store all outcome prices in snapshot, or document limitation and fall back to `fallback_profit * units`.

### M-06 — snapshot.py: fees stored as 0 when total_cost >= 1.0
**File:** `snapshot.py:171`
**Fix:** Compute `fees = gross_profit_from_fee_function - net_profit` directly.

### M-07 — cross_pair_index.py: zero price treated as falsy in `or` chain
**File:** `cross_pair_index.py:84`
**Fix:** Explicit `None` checks instead of `or` fallback.

### M-08 — cross_pair_index.py: _read_cached_price skips staleness check when _ts is 0
**File:** `cross_pair_index.py:72`
**Fix:** Remove `if ts and` guard.

### M-09 — credential_health.py: check_all_platforms awaits coroutines sequentially
**File:** `credential_health.py:75`
**Impact:** 240s serial worst case vs ~30s parallel.
**Fix:** `await asyncio.gather(*[self._check_platform_health(p) for p in self.clients], return_exceptions=True)`.

### M-10 — Gemini _private_request: requests.Timeout swallowed before tenacity can retry
**File:** `gemini_api.py:194`
**Fix:** Add `requests.exceptions.Timeout` to the re-raise except clause.

### M-11 — Gemini _public_request: same requests.Timeout suppression
**File:** `gemini_api.py:231`
**Fix:** Same as M-10.

### M-12 — SuperforecasterClient calls non-existent MetaculusClient method
**File:** `superforecaster_api.py:269`
```python
metaculus_client.get_community_prediction_by_title(market_title)  # AttributeError swallowed
```
**Fix:** Use `search_questions(market_title)` then `get_question_prediction(q_id)`.

### M-13 — Kalshi circuit breaker records success on 4xx/5xx responses
**File:** `kalshi_api.py:166`
**Fix:** Record success only on `resp.status_code < 400`; failure otherwise.

### M-14 — SXBetClient._request raises _RateLimitError to callers with no handler
**File:** `sxbet_api.py:121`
**Fix:** Return `None` when circuit is open, matching `matchbook_api.py` pattern.

### M-15 — cross_mm.py: fee function called with 1-sell_price instead of sell_price
**File:** `scans/cross_mm.py:91`
**Fix:** Call `net_profit_cross_generic(buy_price, sell_price, ...)`.

### M-16 — IndexError when outcomePrices list has fewer than 2 elements in resolution.py
**File:** `scans/resolution.py:174`
**Fix:** `outcome_prices = market.get("outcomePrices") or []; no_price = outcome_prices[1] if len(outcome_prices) > 1 else None`.

### M-17 — correlated: dead Layer-4 floor gate (pass body is a no-op)
**File:** `scans/correlated.py:453`
```python
if (original_spread - live_spread) > layer_floor * original_spread:
    pass  # should be `continue`
```
**Fix:** Replace `pass` with `continue`.

### M-18 — time_decay: guaranteed_gain computed against buy_below_price not current_price
**File:** `scans/time_decay.py:81`
```python
guaranteed_gain = buy_below_price - target_price  # should use current_price
```
**Fix:** `guaranteed_gain = target_price - current_price`.

### M-19 — time_decay / news_snipe: Stage 2 refiners exist but are never called
**File:** `scans/time_decay.py:156`, `scans/news_snipe.py:208`
**Impact:** CLOB ask re-validation and staleness checks skipped in production; higher false-positive rate.
**Fix:** Call `_refine_time_decay_with_prices` and `_refine_news_with_confidence` after Stage 1 scans in `cli.py`/`continuous.py`.

### M-20 — imbalance: Stage 2 collapse check uses magnitude only — direction-flip not detected
**File:** `scans/imbalance.py:82`
**Fix:** Add `if original_ratio * current_ratio < 0: continue` after the magnitude check.

### M-21 — expert_divergence.py: min_forecasters threshold never applied
**File:** `scans/expert_divergence.py:63`
**Fix:** Add `if expert_forecast.get("num_sources", 1) < min_forecasters: continue`.

### M-22 — analytics.py: max_drawdown computes global range, not peak-to-trough
**File:** `scripts/analytics.py:109`
```sql
(max_cumulative_pnl - min_cumulative_pnl) as max_drawdown  -- wrong
```
**Fix:** `MAX(running_peak - cumulative_pnl)` using a window function for `running_peak`.

### M-23 — save_seen trims by lexicographic sort, drops recent low-CIK filings
**File:** `scripts/run_edgar_scan.py:196`
**Fix:** Store accessions with `filed_at` date; trim by date.

### M-24 — validation_report.py: Criterion 2 denominator includes dry_run rows
**File:** `scripts/validation_report.py:106`
**Fix:** Add `AND action NOT IN ('dry_run')` to Criterion 2 query.

### M-25 — Gemini fee verification uses superseded GEMINI_FEE_RATE constant and old formula
**File:** `tests/integration/verify_fees.py:241`
**Fix:** Import `GEMINI_MAKER_RATE`/`GEMINI_TAKER_RATE`; use CFTC 40.6 formula.

### M-26 — test_analytics.py: seeds via sqlite3 but get_strategy_metrics reads via duckdb — incompatible formats
**File:** `tests/test_analytics.py:55`
**Impact:** Every test receives `[]` (swallowed error); assertions fail silently.
**Fix:** Use `duckdb`'s SQLite-attach extension: `conn.execute("ATTACH '...' AS src (TYPE SQLITE)")`.

### M-27 — test_uses_fill_prices_when_available validates broken P&L formula
**File:** `tests/test_continuous.py:497`
```python
expected = 1.0 - (0.41 * 5.0 + 0.46 * 5.0)  # = -3.35 (wrong)
assert realized == pytest.approx(expected)
```
**Fix:** Use normalized sizes or update expected to correct arb formula.

### M-28 — Tautological assertion always passes regardless of LogicalArb presence
**File:** `tests/test_dashboard.py:814`
```python
assert "LogicalArb" in strategy_names or len(strategy_names) >= 0  # always True
```
**Fix:** `assert "LogicalArb" in strategy_names`.

### M-29 — Retry-count assertion too weak: assertGreaterEqual(call_count, 1) passes with 0 retries
**File:** `tests/test_credential_health.py:323`
**Fix:** `self.assertEqual(mock_method.call_count, 2)`.

### M-30 — Conditional assertion guard silently skips INFO severity check
**File:** `tests/test_credential_health.py:179`
**Fix:** Unconditional `assert_called_once()` then `assertIn("INFO", ...)`.

### M-31 — Conditional assertion guard silently skips WARNING severity check
**File:** `tests/test_credential_health.py:207`
**Fix:** Same unconditional approach as M-30.

### M-32 — test_exact_threshold_boundary: docstring says "should fail" but assertion asserts True
**File:** `tests/test_gas_monitor.py:372`
**Fix:** Change assertion to `is False` (with implementation using `<=`) or correct docstring.

### M-33 — Vacuous guard makes test_find_lowest_fee_path_basic non-asserting when function returns falsy
**File:** `tests/test_integration.py:259`
**Fix:** Remove `if result:` guard; assert `result is not None` unconditionally.

### M-34 — TestScanMatchbookBackLay patches live fees module without cleanup
**File:** `tests/test_matchbook_scan.py:196`
**Fix:** Save/restore originals, or use `unittest.mock.patch` as context manager.

### M-35 — Tautological assertion in test_dashboard_refresh_parameter
**File:** `tests/test_rewards_dashboard.py:300`
```python
assert "15" in html_15 or "15" not in html_30  # second clause almost always True
```
**Fix:** `assert "15" in html_15 and "15" not in html_30`.

### M-36 — test_resting_seconds_accuracy: assertion resting >= -1 is always true
**File:** `tests/test_rewards.py:201`
**Fix:** `time.sleep(1.1)` then `assert 0 <= resting <= 3`.

### M-37 — Vacuously true logging assertion in test_ws_stale_detection.py
**File:** `tests/test_ws_stale_detection.py:296`
```python
assert stale_logged or not passed  # `not passed` always True
```
**Fix:** `assert stale_logged`.

### M-38 — mock_external_modules fixture skips mocking pre-loaded modules — test isolation broken
**File:** `tests/test_ws_stale_detection.py:24`
**Fix:** Unconditionally replace and restore (save old value, install mock, restore in teardown).

### M-39 — Trading process auto-loads ~/.claude/.env, mixing Claude AI secrets with trading credentials
**File:** `config.py:9`
```python
load_dotenv(os.path.expanduser("~/.claude/.env"))
```
**Impact:** Any secret in `~/.claude/.env` (including `ANTHROPIC_API_KEY` or any name-colliding credential) is silently injected into the trading process.
**Fix:** Remove this call from both `config.py` and `cli.py`.

### M-40 — Backtest recommendations can override MIN_NET_ROI to near-zero if file is attacker-controlled
**File:** `config.py:867`
**Fix:** Add hard-coded minimum floor (e.g. `0.005`); reject values below it.

### M-41 — External market names injected unescaped into Telegram Markdown messages (link injection)
**File:** `notifier.py:259`
**Impact:** A crafted Polymarket title `[Click here](http://phishing.example)` renders as a clickable phishing link in operator's Telegram alerts.
**Fix:** Remove `parse_mode: "Markdown"` from Telegram payload, or escape all external strings per MarkdownV2 spec.

### M-42 — Thread-unsafe access to _in_memory_cache in CalibrationTracker
**File:** `calibration_tracker.py:104`
**Fix:** Move cache eviction inside `with self._lock:`.

### M-43 — GET /api/* endpoints unauthenticated when DASHBOARD_PASS is unset
**File:** `dashboard.py:245`
**Impact:** With `DASHBOARD_HOST=0.0.0.0` (required for Railway) and forgotten password, all trading data is publicly readable.
**Fix:** Require authentication for non-`/healthz` endpoints when host is non-loopback; enforce at runtime.

### M-44 — GEMINI_DEPOSIT_ADDRESS read without EVM address format validation — address injection risk
**File:** `treasury.py:234`
**Fix:** `assert re.fullmatch(r'0x[0-9a-fA-F]{40}', addr)` before any withdrawal.

### M-45 — SSRF via unvalidated SXBET_API_BASE_URL environment variable
**File:** `sxbet_api.py:16`
**Fix:** Call `assert_public_url(...)` after reading env var (matching `gemini_api.py:86` pattern).

### M-46 — Proxy URLs accepted without SSRF validation across six API clients
**File:** `polymarket_api.py:35` (+ `kalshi_api.py:92`, `betfair_api.py:49`, `smarkets_api.py:48`, `matchbook_api.py:45`, `gemini_api.py:81`, `sxbet_api.py:53`)
**Impact:** Attacker controlling env vars can redirect all outbound API traffic (including HMAC-signed requests) through an internal MITM proxy.
**Fix:** Apply `assert_public_url` to each `*_PROXY_URL` env var.

### M-47 — Unvalidated external signal probability bypasses consensus gate in resolution.py
**File:** `scans/resolution.py:223`
**Impact:** A signal with `prob=5.0` yields `consensus=5.0 > min_probability=0.95` — near-certainty gate bypassed.
**Fix:** Clamp: `if not (0.0 <= prob <= 1.0): continue`; final clamp on returned value.

### M-48 — sxbet.py: missing TypeError when price field is None
**File:** `scans/sxbet.py:136`
**Fix:** `try/except (TypeError, ValueError): continue`.

### M-49 — SSRF via unvalidated --url argument in go_live_check.py
**File:** `scripts/go_live_check.py:48`
**Fix:** Validate scheme (`http`/`https` only) and reject private/loopback IP ranges.

### M-50 — Container runs as root — no USER directive in Dockerfile
**File:** `Dockerfile:43`
**Fix:** Add `RUN addgroup --system arbgrid && adduser --system --ingroup arbgrid arbgrid` and `USER arbgrid`.

### M-51 — Unpinned >= dependency versions allow supply-chain attacks
**File:** `requirements.txt:10`
**Impact:** `fastembed`, `numpy`, `ib_insync`, `duckdb`, `sentry-sdk`, `anthropic`, `PyYAML` — any malicious release ingested automatically on each Docker build.
**Fix:** Pin all production dependencies to exact versions (`==`).

### M-52 — sxbet-proxy nginx forwards arbitrary Authorization headers with no proxy authentication
**File:** `sxbet-proxy/Dockerfile:23`
**Fix:** Add pre-shared token check in nginx, or use Railway private networking.

### M-53 — Missing permissions block in kalshi-lip-scan.yml grants default write token
**File:** `.github/workflows/kalshi-lip-scan.yml:13`
**Fix:** Add `permissions: contents: read`.

### M-54 — SSRF guard opt-out ALLOW_PRIVATE_INTERNAL_URLS=true bypasses all protections globally
**File:** `tests/test_url_guard.py:116`
**Impact:** One env var redirects every API client including HMAC-signed Gemini to internal targets.
**Fix:** Scope opt-out per-variable (e.g. `ALLOW_PRIVATE_POLYGON_RPC_URL`).

### M-55 — No Ethereum address format validation in withdraw_usdc; test confirms 0xabc is accepted
**File:** `tests/test_treasury.py:224`
**Fix:** `re.fullmatch(r'^0x[0-9a-fA-F]{40}$', address)` in `withdraw_usdc` and `validate_config()`.

### M-56 — _calc_realized_pnl unit-scale bug (duplicate confirmation)
**File:** `continuous.py:262`
Same root cause as H-16. Fix: see H-16.

### M-57 — test_uses_fill_prices_when_available validates broken P&L formula (duplicate confirmation)
**File:** `tests/test_continuous.py:508`
Same root cause as M-27. Fix: update expected values.

### M-58 — _brackets_are_complete returns True for finite ranges with no infinite upper bound
**File:** `scans/bracket.py:136`
**Fix:** After loop: `return sorted_brackets[-1]["_bracket_info"]["upper_bound"] == float("inf")`.

### M-59 — _parse_bracket may return wrong numeric bound for 'below' branch
**File:** `scans/bracket.py:59`
**Fix:** Separate direction inference from group indexing.

### M-60 — multi_cross._refine_multi_cross_with_clob: clob_ok always True (dead guard)
**File:** `scans/multi_cross.py:426`
**Impact:** All multi-cross candidates pass the CLOB guard; mid-price candidates reported as CLOB-validated.
**Fix:** Set `clob_ok = False` in the fallback-to-mid-price `else` branch.

### M-61 — smarkets.py: min_depth skips runners with missing quotes, overstating liquidity
**File:** `scans/smarkets.py:80`
**Fix:** Initialize `min_depth = None`; set to `0.0` for runners with no quotes.

### M-62 — fee_promo.py: `or` chain silently drops valid 0.0 price
**File:** `scans/fee_promo.py:54`
**Fix:** `price_a = entry.get("_price_a") if entry.get("_price_a") is not None else entry.get("_yes_price")`.

### M-63 — news_snipe._score_sentiment assigns 0.8 for all matches, contradicting docstring (0.6 for summary)
**File:** `scans/news_snipe.py:178`
**Fix:** Check headline separately (confidence 0.8) from summary (confidence 0.6).

### M-64 — bracket.py: _brackets_are_complete returns True prematurely when non-final bracket has upper_bound == inf
**File:** `scans/bracket.py:130`
**Fix:** Move the `inf` check to after the loop; check last bracket only.

### M-65 — settlement_timing.py: unreachable return None after exhaustive if/elif/else
**File:** `scans/settlement_timing.py:93`
**Fix:** Remove dead `return None`.

### M-66 — insider_pattern.py: get_baseline_flow computes hourly buckets relative to now, not trade time span
**File:** `scans/insider_pattern.py:153`
**Impact:** Empty buckets bias `typical_imbalance_range` upward; every imbalance looks anomalous.
**Fix:** Anchor iteration to `min(timestamps)`.

### M-67 — pnl_report.py: label says 7-day period but default window is 48 hours
**File:** `scripts/pnl_report.py:174`
**Fix:** Change default to `--hours 168` or make label dynamic.

### M-68 — run_all.main(): failed count from all results but passed/skipped from strategy-only
**File:** `tests/integration/run_all.py:248`
**Fix:** Apply consistent scope to all three counters.

### M-69 — test_executor_strategies.py: unreliable sys.path[0] manipulation
**File:** `tests/integration/test_executor_strategies.py:17`
**Fix:** `os.path.abspath(os.path.join(os.path.dirname(__file__), "..", ".."))`.

### M-70 — analytics.py: Sharpe formula duplicate (same as H-07)
**File:** `scripts/analytics.py:104`
Fix: see H-07.

### M-71 — analytics.py: max_drawdown formula validated by test (same as M-22)
**File:** `tests/test_analytics.py:167`
Fix: see M-22; update test expectation.

### M-72 — test_partial_failure_triggers_hedge: mock_hedger never asserted
**File:** `tests/test_concurrent_execution.py:386`
**Fix:** Add `mock_hedger.hedge_position.assert_called_once()`.

### M-73 — test_fetch_balances_failure_logs_and_continues: asserts error IS raised, not that production code handles it
**File:** `tests/test_continuous.py:788`
**Fix:** Exercise real continuous.py exception-handling path.

### M-74 — TestConfigPrecedence: `or` operator silently ignores falsy 0.0 CLI values
**File:** `tests/test_cli.py:182`
**Fix:** Add test cases with `min_profit=0.0`; change logic to `cli_val if cli_val is not None else env_default`.

### M-75 — test_all_14_modes_accepted claims 14 modes but lists 15
**File:** `tests/test_cli.py:161`
**Fix:** Rename to `test_all_15_modes_accepted`.

### M-76 — setup_db fixture missing yield/teardown — TradeDB resource leak
**File:** `tests/test_dashboard.py:467`
**Fix:** Add `yield` then `self.db.close()`.

### M-77 — Teardown unconditionally deletes sys.modules entries without existence guard
**File:** `tests/test_executor_sprint4_branches.py:36`
**Fix:** `if mod_name in sys.modules: del sys.modules[mod_name]`.

### M-78 — test_disclaimer_always_shown discards empty-results output via redundant readouterr()
**File:** `tests/test_display.py:101`
**Fix:** Split into two tests with a single `readouterr()` each.

### M-79 — test_reload_returns_empty_dict_when_unchanged pins only 3 of 9 fee-rate env vars
**File:** `tests/test_fee_backtest.py:34`
**Fix:** Pin all 9 fee-rate env vars in `patch.dict`.

### M-80 — test_login_connects_to_gateway: never calls login(), trivially asserts manually-set attribute
**File:** `tests/test_ibkr_api.py:41`
**Fix:** Replace with real `c.login()` call with mocked `IB.connect`/`IB.isConnected`.

### M-81 — test_proceeds_when_opportunity_id_missing: misleading name (trigger is order_id=None)
**File:** `tests/test_idempotency.py:339`
**Fix:** Rename to `test_proceeds_when_order_id_is_none`.

### M-82 — caplog_fixture defined but never used in test_hedger.py
**File:** `tests/test_hedger.py:49`
**Fix:** Remove unused fixture.

### M-83 — test_higher_gas_price_increases_cost: MATIC price cache not reset, second mock never invoked
**File:** `tests/test_gas_monitor.py:146`
**Fix:** Also reset `monitor._matic_price = None; monitor._matic_price_ts = 0`.

### M-84 — Wrong inline comment: ratio claimed as 0.1 but actual is ~0.048
**File:** `tests/test_imbalance.py:234`
**Fix:** Correct to `# Current ratio = 10/210 ≈ 0.048 < 0.28 → rejected`.

### M-85 — filter_dust mock in test_skips_dust_prices is unreachable dead setup
**File:** `tests/test_kalshi_scan.py:195`
**Fix:** Remove dead patch, or restructure so `filter_dust` is what rejects the candidate.

### M-86 — Dead branch in mock_backall: both if/else return identical values
**File:** `tests/test_matchbook_scan.py:90`
**Fix:** Change `if gross <= 0` branch to return `{"net_profit": 0}`.

### M-87 — test_yes_keywords_detected appends 'd' to already-past-tense keywords
**File:** `tests/test_news_snipe.py:139`
**Fix:** Use natural prose: `f"The proposal was {keyword} by the committee."`.

### M-88 — test_threads_maintain_minimum_interval uses >= (non-strict) instead of >
**File:** `tests/test_polymarket_api.py:215`
**Fix:** `assert sorted_times[i] > sorted_times[i - 1]`.

---

## LOW Findings (78)

### L-01 — TOCTOU race in GasMonitor.get_polygon_gas_gwei (thundering-herd HTTP calls)
**File:** `gas_monitor.py:111` — Add "currently fetching" boolean flag protected by the lock.

### L-02 — backtest.py: realized_profit computed then immediately overwritten (dead code)
**File:** `backtest.py:222` — Remove lines 222-223.

### L-03 — ws_feeds.py: BetfairMarketCache.get_runners acquires lock twice with dead first acquisition
**File:** `ws_feeds.py:769` — Remove the first `with self._lock` block.

### L-04 — alerting.py: check_zero_opp_period_per_strategy defaults unseen strategies to `now`, never fires idle alert
**File:** `alerting.py:354` — Initialize `_strategy_last_opp_time[strategy_type] = now` on first encounter.

### L-05 — TOCTOU race in SignalAggregator.get_divergences (consensus and signals from separate lock acquisitions)
**File:** `signal_aggregator.py:150` — Compute both in a single lock acquisition.

### L-06 — CalibrationTracker.record_resolution invalidates cache outside the lock
**File:** `calibration_tracker.py:104` — Move cache eviction inside `with self._lock:`.

### L-07 — LatencyTracker.get_p95 returns maximum value for N ≤ 20 samples
**File:** `latency_monitor.py:73` — `idx = min(int(math.ceil(N * 0.95)) - 1, N - 1)`.

### L-08 — MarginOptimizer.get_best_platform returns None when all platforms at 100% utilization
**File:** `capital_optimizer.py:150` — Initialize `best_utilization = float('inf')`.

### L-09 — supabase_sync.py: missing timestamp defaults to epoch 1970-01-01
**File:** `supabase_sync.py:49` — Validate `ts` is positive before converting.

### L-10 — market_discovery.py: pair_id split on '|' corrupts IDs when external_id contains delimiter
**File:** `market_discovery.py:350` — Use `json.dumps([a, b])` as separator.

### L-11 — market_discovery.py: DiscoveryResult.rejected undercounts cached non-equivalent pairs
**File:** `market_discovery.py:368` — Increment `rejected` on non-equivalent cache hits.

### L-12 — supabase_sync.py: None platform value produces 'None-rewards' engine tag
**File:** `supabase_sync.py:49` — `platform = row.get('platform') or 'unknown'`.

### L-13 — Kalshi circuit breaker never records failure on 429 / rate-limit
**File:** `kalshi_api.py:164` — `_circuit.record_failure()` before raising `_RateLimitError`.

### L-14 — finnhub_api.py: resp.json() called three times; AttributeError if body is null
**File:** `finnhub_api.py:106` — Parse once: `raw = resp.json(); news_list = raw if isinstance(raw, list) else (raw.get('data', []) if isinstance(raw, dict) else [])`.

### L-15 — polygonscan_api.py: raises requests.Timeout for HTTP 429, misleading callers
**File:** `polygonscan_api.py:98` — Define `_PolygonscanRateLimitError` and raise on 429.

### L-16 — clob_ok dead variable in _refine_multi_cross_with_clob (same as M-60)
**File:** `scans/multi_cross.py:426` — Fix: see M-60.

### L-17 — matchbook.py:91: same min_depth liquidity overstatement as M-61
**File:** `scans/matchbook.py:91` — Fix: see M-61.

### L-18 — LOG_FILE env var passed directly to FileHandler — arbitrary file write via path traversal
**File:** `config.py:77` — Validate path stays within safe directory.

### L-19 — DATA_DIR env var used unsanitized in all file path construction
**File:** `executor.py:218` — Resolve with `os.path.realpath`; assert within allowed root at startup.

### L-20 — LOGICAL_ARB_RULES loaded from CWD-relative path without validation
**File:** `config.py:524` — Resolve relative to `__file__`; add JSON schema validation.

### L-21 — Unbounded POST body read — memory exhaustion DoS
**File:** `dashboard.py:402` — `if content_len > 65_536: send 413; return`.

### L-22 — Dashboard GET endpoints unauthenticated when DASHBOARD_PASS is unset
**File:** `dashboard.py:245` — Remove unconditional `return True`; require auth for financial data endpoints.

### L-23 — No CSRF protection on state-changing POST endpoints
**File:** `dashboard.py:384` — Add `Origin`/`Referer` validation in `do_POST`.

### L-24 — Unauthenticated access (third confirmation)
**File:** `dashboard.py:244` — Fix: see L-22.

### L-25 — Race condition: price_cache read without lock after WS-triggered cross-pair evaluation
**File:** `continuous.py:843` — Take shallow copy `dict(price_cache)` under lock before `evaluate()`.

### L-26 — KalshiRewardTracker.estimate_daily_reward: formula zeroes out for spreads >= 1%
**File:** `market_maker.py:1036` — Remove `* 100` scaling: `(1 - avg_spread) * 0.50`.

### L-27 — Dashboard _handle_validation accesses db.conn directly, bypassing TradeDB lock
**File:** `dashboard.py:747` — Add dedicated read methods to `TradeDB` wrapped with `self._lock`.

### L-28 — _calculate_implied_prob silently ignores days_remaining parameter
**File:** `scans/cross_category.py:185` — Remove parameter or incorporate into probability estimate.

### L-29 — analytics.py: Sharpe formula (third confirmation)
**File:** `scripts/analytics.py:104` — Fix: see H-07.

### L-30 — analytics.py: max_drawdown formula (third confirmation)
**File:** `scripts/analytics.py:109` — Fix: see M-22.

### L-31 — twitter_api.py: get_market_sentiment uses deprecated datetime.utcnow()
**File:** `twitter_api.py:284` — Replace with `datetime.now(timezone.utc)`.

### L-32 — position_sizer.py: Kelly formula degenerates to 1.0 for all pure-arb opportunities
**File:** `position_sizer.py:263` — For arb: `odds = (1.0 - total_cost) / total_cost`.

### L-33 — RSA private key validation unconditionally disabled for Kalshi credentials
**File:** `kalshi_api.py:52` — Apply strict validation first; fall back to `unsafe_skip_rsa_key_validation=True` only on `ValueError`.

### L-34 — RSA private key validation silently disabled (second confirmation)
**File:** `kalshi_api.py:52` — Fix: see L-33.

### L-35 — Decision log file handle opened at constructor time with no error handling
**File:** `executor.py:220` — Wrap in try/except; register SIGTERM handler to flush/close.

### L-36 — test_zero_balance_returns_zero: misleading name — asserts 5.0, not 0
**File:** `tests/test_risk_manager.py:342` — Rename to `test_zero_balance_skips_constraint`.

### L-37 — Module isolation fixture in test_nway_arb.py: no teardown
**File:** `tests/test_nway_arb.py:22` — Add `sys.modules.pop('scans.triangular', None)` after `yield`.

### L-38 — test_filters_below_threshold: source 'a' also exceeds threshold but comment claims only 'c'
**File:** `tests/test_signal_aggregator.py:126` — Rework signal values; add correct assertions.

### L-39 — test_drops_opp_when_clob_shows_no_profit: patch of scanner.net_profit_binary_internal is a no-op
**File:** `tests/test_scanner.py:340` — Remove dead assignment, or use `patch('scans.binary.net_profit_binary_internal', ...)`.

### L-40 — test_single_outcome_event_skipped: slice on already-single-element list is a no-op
**File:** `tests/test_scanner.py:247` — Construct multi-outcome event first, then trim.

### L-41 — test_consensus_side_no_when_low uses semantically inverted threshold without comment
**File:** `tests/test_time_decay.py:349` — Add explicit comment; add sister test documenting the limitation.

### L-42 — Duplicate test methods test_rejects_error_transactions and test_returns_none_for_reverted
**File:** `tests/test_whale_copy.py:161` — Remove `test_returns_none_for_reverted`.

### L-43 — test_fresh_feed_is_healthy: first patch(time.time) block wraps plain dict assignment — dead patch
**File:** `tests/test_ws_stale_detection.py:102` — Remove first `with patch("time.time", ...)`.

### L-44 — test_skips_empty_tokens: doesn't assert None and "" are absent from _poly_token_ids
**File:** `tests/test_ws_feeds.py:325` — Add `assert None not in fm._poly_token_ids` and `assert "" not in fm._poly_token_ids`.

### L-45 — traces_sample_rate=1.0 in non-production sends 100% of traces to Sentry
**File:** `sentry_init.py:18` — Default to `0.0` for non-production.

### L-46 — TransferResult.error exposes internal exception message in HTTP response
**File:** `treasury.py:197` — Return `"Transfer failed — see server logs"` in HTTP response.

### L-47 — Silent fallback to unauthenticated Polygonscan demo key 'YourApiKeyToken'
**File:** `polygonscan_api.py:86` — Log clear warning; return empty list when unconfigured.

### L-48 — scans/sxbet.py: unguarded key access crashes scan thread (first scan function)
**File:** `scans/sxbet.py:55` — `.get()` + `try/except (TypeError, ValueError): continue`.

### L-49 — scans/betfair.py: uncontrolled float() conversion on back prices can raise ValueError
**File:** `scans/betfair.py:79` — Wrap in `try/except ValueError`.

### L-50 — Platform-supplied market titles logged without sanitization (log injection)
**File:** `scans/multi_cross.py:314` — Strip control characters before logging.

### L-51 — Unbounded external news text — potential large-payload DoS
**File:** `scans/news_snipe.py:134` — Truncate: `headline[:500]`, `summary[:2000]`.

### L-52 — Symbols extracted from market questions passed unsanitized to Finnhub API
**File:** `scans/news_snipe.py:80` — Post-filter against stop-word list; require `len >= 2`.

### L-53 — Hardcoded POLYMARKET_CLOB_ADDRESS duplicated in two files
**File:** `scans/whale_copy.py:15` — Import from `whale_copy_decoder.py` as single canonical source.

### L-54 — _news_timestamp from external source defeats staleness check with far-future values
**File:** `scans/news_snipe.py:164` — Clamp at store time: `0 < ts <= current_time + 300`.

### L-55 — Dashboard password passed via --password CLI argument, exposed in process listing
**File:** `scripts/go_live_check.py:88` — Use `os.getenv('DASHBOARD_PASS')` or `getpass.getpass()`.

### L-56 — Unvalidated external price data from CoinGecko used directly in trading decisions
**File:** `scans/cross_category.py:126` — Validate `isinstance(price, (int, float)) and price > 0`; add plausibility bounds.

### L-57 — Path traversal via --db-path in analytics.py passed to DuckDB
**File:** `scripts/analytics.py:68` — Resolve and restrict within `DATA_DIR`.

### L-58 — SSRF via unvalidated URL from remote Atom feed entry in run_edgar_scan.py
**File:** `scripts/run_edgar_scan.py:229` — Validate host equals `www.sec.gov` before secondary fetch.

### L-59 — subprocess spawned with full parent environment including live production secrets
**File:** `tests/test_backtest_tuning.py:208` — Build minimal env containing only required test variables.

### L-60 — Failure assertion exposes subprocess stderr (may contain secret-referencing log lines)
**File:** `tests/test_backtest_tuning.py:213` — `f"subprocess failed: {proc.stderr[:200]!r}"`.

### L-61 — Internal exception detail leaked to HTTP clients via {"error": str(e)}
**File:** `dashboard.py:379` — Return `{"error": "internal error"}`; log full exception server-side.

### L-62 — Unbounded POST body read (second endpoint confirmation)
**File:** `dashboard.py:402` — Fix: see L-21.

### L-63 — Hardcoded /tmp path for DiscoveryCache in tests
**File:** `tests/test_market_discovery.py:190` — Use `tmp_path` fixture.

### L-64 — Relative fixture path depends on pytest invocation directory
**File:** `tests/test_kalshi_api.py:526` — `Path(__file__).resolve().parent / 'fixtures' / ...`.

### L-65 — SSRF via proxy URL (additional confirmation for M-46)
**File:** `polymarket_api.py:34` — Fix: see M-46.

### L-66 — Unvalidated CoinGecko price data (second endpoint confirmation)
**File:** `scans/cross_category.py:120` — Fix: see L-56.

### L-67 — Dashboard GET endpoints access db.conn directly (second confirmation)
**File:** `dashboard.py:747` — Fix: see L-27.

### L-68 — Hardcoded default username 'admin' for dashboard Basic Auth
**File:** `config.py:757` — Remove `'admin'` default; raise `ConfigError` when non-loopback with default username.

### L-69 — Unvalidated subreddit and sort parameters interpolated into Reddit API URL path
**File:** `reddit_api.py:156` — Validate subreddit against `^[A-Za-z0-9_]{1,21}$`.

### L-70 — Unused TWITTER_API_KEY and TWITTER_API_SECRET imported into module scope
**File:** `twitter_api.py:20` — Remove unused imports.

### L-71 — DuckDB connection string accepts MotherDuck URIs (md:) — SSRF-adjacent
**File:** `scripts/analytics.py:68` — Validate path is plain `.db` filesystem path.

### L-72 — Output directory from --output in tune.py allows write to arbitrary paths
**File:** `scripts/tune.py:209` — Assert output path is within `DATA_DIR` using `pathlib.Path.is_relative_to`.

### L-73 — TOCTOU race in _authenticate() in reddit_api.py — double OAuth and header data race
**File:** `reddit_api.py:94` — Protect entire check-and-refresh sequence with `self._lock`.

### L-74 — discovery_cache.json read from arbitrary env-var path without validation
**File:** `market_discovery.py:187` — Resolve and confine to `DATA_DIR`; validate loaded JSON structure.

### L-75 — _revalidate_triangular silently proceeds with stale scan prices when WS feed returns None
**File:** `executor.py:1184` — `if fresh_yes is None or fresh_no is None: raise _RevalidationAPIError('triangular feed stale')`.

### L-76 — test_partial_failure_triggers_hedge: double-patch of HEDGE_ENABLED (decorator + context manager)
**File:** `tests/test_concurrent_execution.py:386` — Remove redundant decorator-level patch.

### L-77 — test_skips_dust_prices: dead filter_dust mock setup (same as M-85)
**File:** `tests/test_kalshi_scan.py:195` — Fix: see M-85.

### L-78 — _revalidate_triangular stale feed (second confirmation)
**File:** `executor.py:1184` — Fix: see L-75.

---

## Unverified — re-run to confirm

None. All 182 findings confirmed (3-lens adversarial vote); 0 errored out.

---

## Remediation Order

**Tier 1 — Production-blocking (fix before next deploy):**
H-01, H-02 (deadlocks), H-08 (CI blindness), H-03/H-14 (cross-all filter hole), H-04/H-05 (triangular refinement keys), H-16/M-56/M-57 (P&L formula + tests), H-09/H-10 (treasury double-spend), H-11 (FeedManager price_cache), H-12 (WhaleCopy counter), H-15 (recovery.py token_id), H-06 (correlated CLOB keys), H-13 (circuit breaker on 4xx)

**Tier 2 — Before next live trade (correctness):**
M-01 (fee weight inversion), M-02 (daily P&L mixing), M-03 (stale feed default), M-13 (Kalshi circuit success on 5xx), M-19 (Stage 2 refiners not called), M-17 (correlated dead gate), M-04 (loss streak equality), M-07/M-08 (zero-price falsy checks)

**Tier 3 — Security (next sprint):**
M-39 (auto-load ~/.claude/.env), M-41 (Telegram link injection), M-43/L-22 (dashboard auth), M-44/M-55 (EVM address validation), M-45/M-46/L-65 (proxy SSRF), M-50 (root container), M-51 (unpinned deps), M-52 (nginx auth), M-53 (GitHub token), M-54 (SSRF opt-out scope)

**Tier 4 — Metrics and analytics correctness:**
H-07/M-70/L-29 (Sharpe ratio — 3 confirmations), M-22/M-71/L-30 (max_drawdown — 3 confirmations), M-18 (time_decay gain formula), M-21 (min_forecasters gate)

**Tier 5 — Test quality (ongoing):**
M-26 (duckdb/sqlite format), M-28/M-35/M-37 (tautological assertions), M-29/M-30/M-31/M-33 (weak/vacuous assertions), all remaining L-3x through L-7x test findings
