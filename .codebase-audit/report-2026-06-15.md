# Codebase Audit Report — arbgrid / polymarket-arb-scanner
**Date:** 2026-06-15  
**Dimensions:** bugs, security  
**Scope:** . (all ~95 source files)  
**Method:** Scout → 16 parallel finders → 9 adversarial verifiers (3 lenses × 3 batches) → synthesis  
**Verification rule:** Majority ≥ 2/3 lenses required to confirm  

---

## Summary

| Severity | Count |
|----------|-------|
| Critical | 1 |
| High | 5 |
| Medium | 8 |
| Low | 8 |
| Refuted | 5 |
| **Total confirmed** | **22** |

---

## CRITICAL

### C-1 · `scans/bracket.py:298` — CLOB refinement inverts arb condition; drops ALL profitable bracket arbs
**Votes:** correctness=TRUE, impact=TRUE/critical, reproduce=TRUE — 3/3 confirmed

The first-pass scan correctly keeps opportunities where `spread = 1.0 - total_cost >= min_spread` (i.e., total_cost < 1.0 = profitable). The CLOB refinement step then re-prices with ask prices, computes `overround = total_cost - 1.0`, and does `if overround < 0: continue` — which drops exactly the profitable cases (overround < 0 ⟺ total_cost < 1.0). Only unprofitable overround cases survive. **BracketArb strategy (Strategy #31) is completely non-functional.**

```python
# scans/bracket.py:296-299
total_cost = sum(prices)
overround = total_cost - 1.0
if overround < 0:   # BUG: should be `if overround >= 0`
    continue
```

**Fix:** Invert condition to `if overround >= 0: continue` (keep underround, drop overround).

---

## HIGH

### H-1 · `latency_monitor.py:372` — Non-reentrant lock deadlock in `should_route_externally`
**Votes:** correctness=TRUE/critical, impact=TRUE/high, reproduce=TRUE/high — 3/3 confirmed

`should_route_externally()` acquires `self._lock` (line ~366) then calls `get_best_region()` (line ~372), which also tries to acquire the same `threading.Lock`. Python's `threading.Lock` is not reentrant — this deadlocks any thread on first invocation when `GEOGRAPHIC_LATENCY_ENABLED=true` and `_region_latencies` is populated. The feature defaults to false but the deadlock is certain when enabled.

**Fix:** Use `threading.RLock()` instead of `threading.Lock()` in `LatencyMonitor.__init__`, or restructure `get_best_region()` to accept a pre-acquired lock.

---

### H-2 · `scans/multi_cross.py:254` — `_parallel_fetch_kalshi()` called with wrong args; `--mode multi-cross` always crashes silently
**Votes:** correctness=TRUE/high, impact=TRUE/high, reproduce=TRUE/high — 3/3 confirmed

`scans/helpers.py`'s `_parallel_fetch_kalshi(kalshi_client, tickers: list[str], ...)` requires a `tickers` argument and returns a `dict`. But `multi_cross.py:254` calls it as `_parallel_fetch_kalshi(kalshi_client)` (missing `tickers`) and unpacks the result as a 3-tuple. This branch fires when `--mode multi-cross` is run: `cli.py:129` only fetches `kalshi_data` for modes `all/kalshi/cross/spread`, not `multi-cross` — so `kalshi_data` is `None`, the `elif kalshi_client:` branch executes, raises `TypeError`, silently swallowed by `except Exception`.

```python
# scans/multi_cross.py:254
kalshi_events, kalshi_by_event, kalshi_event_titles = _parallel_fetch_kalshi(kalshi_client)
# _parallel_fetch_kalshi(client, tickers, ...) → wrong signature; raises TypeError
```

**Fix:** Pass `tickers` argument or propagate `kalshi_data` correctly to `scan_multi_cross()`.

---

### H-3 · `continuous.py:257-259` — `_calc_realized_pnl` always returns wrong P&L for real trades
**Votes:** correctness=FALSE, impact=TRUE/high, reproduce=TRUE/medium — 2/3 confirmed

The trades table `size` column stores dollar amounts (executor passes `max_trade_size`, default $5.00). `total_fill_cost = sum(fill_price * size)` computes actual dollars spent, e.g., $0.48×$5 + $0.49×$5 = $4.85 for a $5 two-leg arb. Then `return 1.0 - total_fill_cost = 1.0 - 4.85 = -$3.85` — wildly wrong (actual P&L ≈ +$0.15). Every arbitrage position settled via the no-`winning_side` path (cross, betfair, smarkets, sxbet, matchbook, gemini, ibkr) records massive negative P&L in `trades.db`.

```python
# continuous.py:257-259
if winning_side is None:
    return 1.0 - total_fill_cost  # BUG: works only when total_fill_cost ≈ $0.97 (1-contract arbs)
```

**Fix:** Change to `return total_payout - total_fill_cost` where `total_payout = sum(t["size"] / fill * fill)` = sum of dollar sizes on winning legs, or: divide by trade size first to normalize to per-contract basis.

---

### H-4 · `scans/cross.py:621` — `refined_out` declared but never used; CLOB filter is a no-op for pruning
**Votes:** correctness=TRUE/high, impact=TRUE/medium, reproduce=TRUE/medium — 3/3 confirmed

`_refine_cross_all_with_clob()` declares `refined_out = []` at line 621 but never appends to it and the function has no return statement. Opportunities that fail the CLOB check (missing data, unparseable price, or `net_profit < min_profit`) are never removed from the original list — only good opportunities get their fields updated in-place. Stale mid-price arbs that fail real-price validation silently survive.

```python
# scans/cross.py:621
refined_out = []  # declared but never used
for o in pm_opps:
    ...
    if best_r["net_profit"] >= min_profit:
        o["net_profit"] = best_r["net_profit"]  # mutates in-place only
# function ends with no return
```

---

### H-5 · `scans/triangular.py:414` — `_price_b` always 0 for TriangularCross; profit recalculated with phantom leg
**Votes:** correctness=TRUE/high, impact=FALSE (0.85), reproduce=TRUE/high — 2/3 confirmed

`scan_triangular()` never sets `_price_b` on the opportunity dicts it produces. When `_refine_triangular_with_clob()` runs, `other_price = o.get("_price_b", 0)` defaults to 0. `net_profit_triangular(pm_ask, 0, pa, pb)` calculates `gross_spread = 1 - pm_ask - 0 ≈ 1`, producing wildly inflated profit figures. Mispriced candidates pass the `>= min_profit` filter and can be executed.

```python
# scans/triangular.py:414
other_price = o.get("_price_b", 0)  # always 0 for TriangularCross opps
```

---

## MEDIUM

### M-1 · `db.py:557` — `get_strategy_pnl()` double-counts net_profit for multi-leg arbs
**Votes:** correctness=TRUE, impact=TRUE, reproduce=TRUE — 3/3 confirmed

The SQL query JOINs `trades` to `opportunities` and sums `o.net_profit` — since `net_profit` is on the `opportunities` table and there are N trade rows per opportunity, the `SUM` inflates net_profit by N. A 2-leg cross-platform arb gets counted twice; a 3-leg triangular arb gets tripled. The strategy P&L dashboard is systematically overstated.

```sql
-- db.py:557 (approx)
SELECT o.type, SUM(o.net_profit)  -- inflated: summed once per trade leg
FROM trades t JOIN opportunities o ON t.opportunity_id = o.id
GROUP BY o.type
```

**Fix:** Use `SELECT o.type, SUM(sub.net_profit) FROM (SELECT DISTINCT opportunity_id, net_profit FROM opportunities ...) sub`.

---

### M-2 · `continuous.py:1517` — `FINNHUB_API_KEY` NameError silently disables news-snipe
**Votes:** correctness=TRUE, impact=TRUE, reproduce=TRUE — 3/3 confirmed

`FINNHUB_API_KEY` is not imported in `continuous.py` module scope (not in the `from config import (...)` block). At line 1517 it is referenced bare inside a `try/except Exception` block which swallows the `NameError` and logs at DEBUG level. News-snipe strategy (`--mode news-snipe` or `all`) silently fails even when `NEWS_SNIPE_ENABLED=true` and `FINNHUB_API_KEY` is correctly set in env.

**Fix:** Add `FINNHUB_API_KEY` to the `from config import (...)` block at the top of `continuous.py`.

---

### M-3 · `hedger.py:178` — hedge contract count uses current bid price instead of original fill price
**Votes:** correctness=TRUE, impact=TRUE, reproduce=TRUE — 3/3 confirmed

`count = max(1, int(size / bid))` uses the current live bid from the order book, not `fill_price`. If the market has moved since the original fill (e.g., fill_price=0.60, current bid=0.55, size=$10), the wrong contract count is placed: 18 contracts vs correct 16. Hedge is mis-sized whenever price has moved between fill and hedge execution.

---

### M-4 · `position_sizer.py:354` — `_extract_net_roi` returns 83% for "0.83%" strings; Kelly massively over-sizes positions
**Votes:** correctness=TRUE, impact=TRUE/high, reproduce=TRUE — 3/3 confirmed

The guard `if abs(val) > 1: return val / 100.0; return val` fails for sub-1% percentage strings: "0.83%" → `val=0.83` → `abs(0.83) <= 1` → returns `0.83` (83% ROI) instead of `0.0083` (0.83%). This causes Kelly criterion to dramatically over-size positions for any opportunity with net_roi stored as a formatted percentage string below 1%.

---

### M-5 · `market_maker.py:1036` — `KalshiRewardTracker.estimate_daily_reward` always returns 0.0
**Votes:** correctness=TRUE, impact=TRUE/low, reproduce=TRUE — 3/3 confirmed

`avg_spread` is stored as a decimal fraction (e.g., 0.03 for 3%). The formula `(1 - avg_spread * 100)` converts it to percentage form (3.0) then subtracts from 1, giving −2.0. `max(0.0, ...)` clamps to 0.0 for any spread > 1%. Since `MM_MIN_SPREAD` defaults to 0.03 (3%), this always returns 0.0 in practice. Kalshi market making reward estimates are dead.

---

### M-6 · `reddit_api.py:156` — unsanitized subreddit name allows URL path traversal  *(security)*
**Votes:** 3/3 confirmed, medium

Subreddit names from API response or config are concatenated directly into the URL path without sanitization. A subreddit name like `../../../search?q=` would traverse to a different Reddit API endpoint. Malicious subreddit data could redirect the client.

---

### M-7 · `dashboard.py:264` — non-constant-time password comparison exposes timing oracle  *(security)*
**Votes:** 2/3 confirmed (impact lens: FALSE; correctness + reproduce: TRUE), medium

`user == DASHBOARD_USER and pwd == DASHBOARD_PASS` uses Python's string equality, which short-circuits on the first differing character. An attacker with network access to the dashboard can distinguish correct vs. incorrect password prefixes via response time. Use `hmac.compare_digest()` instead.

---

### M-8 · `finnhub_api.py:80` — Finnhub API key sent as URL query parameter; logged by infra  *(security)*
**Votes:** 3/3 confirmed, medium

`params={"token": self.api_key}` causes the API key to appear in the request URL, which is logged by HTTP proxies, Nginx access logs, and Railway's log aggregator. Any log access exposes the key.

**Fix:** Move to `headers={"X-Finnhub-Token": self.api_key}` (supported by Finnhub API).

---

## LOW

### L-1 · `fees.py:34` — `select_fees` EV weights slightly swapped
**Votes:** 3/3 confirmed, low (impact lens downgraded from medium to low after analysis)

`prob_a * case_b_fees + (1.0 - prob_a) * case_a_fees` applies prob_a to the wrong fee case. The actual error is proportional to `(case_a_fees - case_b_fees) * (1 - 2*prob_a)`, typically < 0.2% of trade value. Borderline arbs near the minimum ROI threshold may be incorrectly filtered.

---

### L-2 · `scans/time_decay.py:81` — `guaranteed_gain` computed backwards (display only)
**Votes:** 3/3 confirmed, low

`guaranteed_gain = buy_below_price - target_price` can be zero or negative for valid opportunities. Correct formula is `target_price - current_price`. However `_guaranteed_gain` is not used by the executor for execution decisions — impact is limited to misleading log output.

---

### L-3 · `continuous.py:849` — `_seq_counter` non-atomic despite misleading GIL comment
**Votes:** correctness=TRUE, impact=FALSE, reproduce=TRUE — 2/3 confirmed, low

`_seq_counter += 1` is not guaranteed atomic in CPython. The comment claiming GIL safety is incorrect (augmented assignment involves separate LOAD and STORE bytecodes). In practice the collision frequency is negligible and the only consequence is duplicate sequence numbers affecting priority queue tie-breaking order — no data loss, no position corruption.

---

### L-4 · `scans/correlated.py:453` — `layer_floor` gate is dead code (`pass` instead of `continue`)
**Votes:** 2/3 confirmed (reproduce: FALSE), low

```python
if (original_spread - live_spread) > layer_floor * original_spread:
    pass  # already handled by spread_collapse gate
```
The `pass` is a no-op; the comment implies it should `continue` but the spread_collapse gate above already handles this case. Dead code clutters the filter logic.

---

### L-5 · `config.py:9` — silent secondary credential source from `~/.claude/.env`  *(security)*
**Votes:** confirmed, low

`load_dotenv(os.path.expanduser("~/.claude/.env"))` loads a second `.env` file that is not documented and not covered by secret scanning. Values silently override the primary `.env`. The path is inside the Claude tool's own directory, which may have broader read access than intended.

---

### L-6 · `kalshi_api.py:46` — `unsafe_skip_rsa_key_validation=True` bypasses CRT parameter checks  *(security)*
**Votes:** confirmed, low

The RSA private key is loaded with validation disabled, allowing malformed keys (missing CRT parameters) to be used silently. This could result in slow signing or incorrect signatures without error.

---

### L-7 · `executor.py:220` — decision log opened without 0o600 permissions; world-readable  *(security)*
**Votes:** confirmed, low

The trade decision log file is created with default umask permissions (typically 0o644), making it readable by all users on the system. The file records opportunity details, profit figures, and strategy identifiers.

---

### L-8 · `dashboard_ui.py` `/api/pause` + `/api/resume` — missing CSRF protection  *(security)*
**Votes:** 2/3 confirmed (impact lens: FALSE — browsers don't auto-send Basic Auth cross-origin), low

The kill-switch endpoints have no CSRF token or Origin header check. Practical exploitability is low because browsers don't automatically include Basic Auth credentials for cross-origin requests, but any session using cookie-based auth or a logged-in browser tab could be targeted by a CSRF link.

---

## REFUTED (do not fix)

| Finding | Reason |
|---------|--------|
| `scans/resolution.py:128` timestamp ÷1000 | All API paths use ISO string format; integer branch is unreachable in practice (2/3 FALSE) |
| `treasury.py:172` unhandled IntegrityError | `db.log_transfer()` already catches `sqlite3.IntegrityError` internally (3/3 FALSE) |
| SSRF via `WEBHOOK_URL`, `GJO_API_URL`, `INFER_API_URL` | Operator-configured env vars, not user-supplied runtime input (3/3 FALSE) |
| SQL injection in `db.py` placeholder join | `",".join("?" for _ in opp_ids)` produces only safe `?` placeholders (3/3 FALSE) |
| `continuous.py:849` `_seq_counter` race (as critical) | Theoretical; GIL makes collision negligible in practice; downgraded to L-3 |

---

## Under-examined areas (completeness gaps)

The following modules received no findings from this audit pass and warrant a targeted follow-up:
- **`ws_feeds.py`** — WebSocket reconnect logic, subscription management, shared price cache writes under asyncio/threading boundary
- **`recovery.py`** — orphan position reconciliation after crash; potential for double-execution if recovery races with executor restart
- **`risk_manager.py`** — daily loss limit reset logic, per-market lock state after recovery
- **`backtest.py`** — replay engine slippage assumptions, fee model parity with live scanner

---

## Fix Priority Queue

1. **C-1** `scans/bracket.py:298` — invert `overround < 0` → `overround >= 0` (one-line fix, restores BracketArb)
2. **H-3** `continuous.py:257` — fix `_calc_realized_pnl` to normalize by contract size, not dollar size
3. **H-1** `latency_monitor.py` — change `threading.Lock` → `threading.RLock` in `LatencyMonitor`
4. **H-2** `scans/multi_cross.py:254` — pass `tickers` arg to `_parallel_fetch_kalshi`
5. **M-2** `continuous.py` imports — add `FINNHUB_API_KEY` to `from config import (...)`
6. **H-4** `scans/cross.py:621` — return `refined_out` or switch to in-place list pruning
7. **H-5** `scans/triangular.py` — set `_price_a`/`_price_b` in `scan_triangular()` output
8. **M-1** `db.py:557` — fix `get_strategy_pnl()` SQL to avoid JOIN-multiplied net_profit
9. **M-4** `position_sizer.py:354` — fix boundary: `if abs(val) > 1:` should be `if abs(val) >= 1:`
10. **M-8** `finnhub_api.py:80` — move API key to `X-Finnhub-Token` header
